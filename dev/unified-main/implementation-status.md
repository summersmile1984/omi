# Three-track delivery status

The delivery target is one `main`, two deployment targets, and white-label
clients. The [2026-09-04 audit](audit-2026-09-04/01-self-host-action-plan.md)
defines the acceptance criteria. A local implementation or unit-test pass is
not a release, a signed client, or a completed product loop.

**2026-09-05 Eddy update:** the requested brand/SVGs and Developer ID macOS package
have been built, signed and opened in the real native UI. The actual Cloudflare
account has all 18 Eddy data resources and five verified policies. Eight Workers
remain absent and both D1 databases have no business schema; canonical `apply`
refuses the still-missing CF-4/CI-1/prior-schema runners before mutation. See the
[first production delivery evidence](implementation-2026-09-05/eddy-production-progress.md)
for exact artifacts, failed attempts and qualification boundaries. No production
application release or complete client business loop is claimed.

The subsequent [first-release schema package](implementation-2026-09-05/eddy-first-release-schema.md)
implements the fixed schema runner and passed five cases against freshly observed
Eddy Worker absence/empty D1 authorities plus actual frozen SQL execution. Its
deployed/retained-version limitations remain explicit. The remaining absent
publish runners are CF-4 and CI-1; no Worker or business schema has been published.

Earlier integrated audit: candidate branch `codex/unified-delivery`; the implementation source
tip reviewed is `0d4f71b4ab` (the following audit record is documentation
only). It is a reviewable local candidate: nothing in it has been pushed, merged, or
deployed to production.

`upstream/main` at `c4880cd5f6` is an ancestor of the reviewed implementation
tip. At that audit, the branch was `0 264` relative to `upstream/main`;
`scripts/fork/upstream_sync_plan.py --base
HEAD --upstream upstream/main` reports no merge conflicts. On 2026-09-05,
`python3 scripts/fork/check-upstream-touch.py --base upstream/main --head HEAD
--upstream-ref upstream/main --json` passed with zero violations. Its only two
upstream files are the approved `app/lib/flavors.dart` hook and the macOS update
note, both within their allowlist budgets. The former server provider edits now
live behind the fork registry, Server OS-only packages use
`backend/requirements-fork.txt`, and macOS native authentication is staged from
fork-owned sources.

This closes the one-fork source-boundary work locally. It does not claim a
remote Cloudflare qualification, a signed client release, or a production
Server OS deployment; those remain release gates rather than source-tree work.
Fresh local target acceptance on 2026-09-05 passed
`bash deploy/self-host/ci/product.sh` and
`bash deploy/cloudflare/ci/product.sh`: the latter's isolated Worker/D1 trace
records all 8 core, 6 recording, 11 chat, and 2 public-share cases as passing.
The closure audit at this tip also reran the upstream-touch guard and the
Flutter, macOS, and Electron stage suites; its exact scope, evidence, and the
macOS disk-capacity limitation are recorded in
[`closure-audit-0d4f71b4ab.md`](implementation-2026-09-05/closure-audit-0d4f71b4ab.md).

| Package | Owner | Status / next evidence |
| --- | --- | --- |
| SH-1 startup, profile, migration | Server | Integrated `3eaad62568`; actual amd64 API/4 consumers, fresh/repeat/v2-upgrade PG, fatal bad-config/consumer tests passed |
| AUTH-1 shared identity | Integration | Integrated through `0cbdc4a32b`: shared JWT/live sessions/password policy, PG password upgrade, selected identity deletion, encoded imported UIDs and canonical import ordering. Real PG/workerd/browser checks, separate ARM64/AMD64 Auth images, deletion lost-response/retry/zero-residual proof and old-image import failure/new-image reconciliation passed. macOS and Android consumers are integrated; bounded JWT cache skew is shared by Web/macOS (`a5f95a8e85`). Selected profile reads are integrated (`fb1e00642d`): captured conversation/model consumers use the existing Better Auth internal authority, with real Linux HTTP/name/absence evidence and zero Firebase calls. OAuth and complete product-family erasure remain |
| WL-1 brand input/render/check | White-label | Implemented and integrated as `0bb1c37ce9`; 35 brand + 7 profile tests and actual target CLI fixtures passed |
| CF-1 route inventory | Cloudflare | Integrated `526fae377e`; CF-2 corrected live-model ownership: 612 backend identities = 576 staging-owned + 36 blocked. Desktop prompts passed real workerd/D1 |
| SH-2 storage and retrieval | Server | Integrated through `822b9d4163` plus AUTH deletion `2965b29656`/`87417d7a5e`: PG completion receipt, authoritative MinIO/Qdrant adapters, five captured provider fences and exclusive wipe lease. Real standard amd64 PG/Qdrant/MinIO/Redis evidence passed. Direct PG/background writes and stale snapshots are fenced (`b7a1939005`), with actual PostgreSQL lock/serialization tests. Schema v6 admits onboarding, legal-hold and canonical-memory dynamic collection owners (`9f8b688209`). Upstream's feedback ledger added top-level event and daily-report owners, so `73de63d21c` adds immutable schema v7 and its composite index; the fresh local PostgreSQL v6→v7 upgrade preserves a feedback row and passes all 29 transaction-semantic tests. Unverified external/non-UID families remain |
| SH-3 providers and egress | Server | Integrated `683aa04d0e`/`5c8cc253da`: one BGE-M3 model/artifact/dimension contract, real CPU Ollama embeddings, bound Qdrant metadata and Chinese retrieval, Tasks persistence and explicit disabled STT/TTS/push. Real HTTP/WS and synchronous/asynchronous BYOK notification paths passed with zero FCM/token/cooldown effects. Pinned local SenseVoice/Kokoro speech is integrated (`c8f13c5e35`): actual CPU PCM/multipart/TTS/PTT/listen passed. Terminal usage settles accepted audio once (`ab47ce1e68`), and text/empty keepalives cannot extend the audio-idle deadline (`4719476f77`); real Redis usage and 30-second idle evidence passed. Upstream now reserves PTT budget atomically at connect; `c8414bef6f` brings the white-label local owner to the same reserve/settle/refund contract, preserves its 60-second local cap, and emits shared fallback telemetry when Redis reservation is unavailable. Local PTT's 21 wire/error-path tests and the upstream duration suite pass. Local Qwen completion/schema/stream/tool and actual API greeting have passed in the development target. PG nested usage repair `d0dcd36ae8` passes actual usage/question persistence and explicit-contention tests, plus 25 upstream/3 named fork checks; fresh standard AMD64 image with exact Python source hashes and shared HTTP8 now passes. Actual Qwen structure processing persisted 2036 input/335 output tokens and one call through the production usage owner. Redis finalization admission/generation publication is integrated (`3e56dd5f3c`), with actual public enqueue and worker entry. The profile-owned Qwen/Ollama runtime, provisioning, bounded transport, schema/tool path and persona streaming consumers are integrated (`2146820175`); the focused combined gate passes 31 tests. The in-progress runtime uses measured Q8 cache precision and four CPU threads; a new recording passed ASR/persistence and its first structure call without OOM, but full finalization/conversation/memory remains unfinished. Authenticated retry counts and rejection retention are integrated (`78b9ae9913`/`79e22a8121`), with actual Redis/API/PG wire evidence. Canonical replacement now passes the isolated PostgreSQL transaction after v6 admission. `dcd890cf39` adds an explicit supervised canonical-memory maintenance/outbox worker: an isolated Linux source-replacement proof emitted and delivered four events, moved Qdrant from zero to one owner document, removed one stale Typesense document, and returned one unique canonical result. Long-term Ledger keyword upsert, a fresh immutable image, release qualification, and old-registry backfill remain unproved. |
| INTEGRATION-1: CLIENT-1 Web + CF-2 + WEB-1 | All | Integrated Web `8404f58a4d`, dual builder `a0b600883b`, request ownership `30998638b2`, capabilities `9de1dd5d26`/`b90e63de5c`, CF realtime `cf5d5fe308`. Both targets passed real browser signup/restore/logout and Tasks create/reload/complete; cross-user denial passed. Both API/Core restart checks retained task state and actual browser ASR transport passed with synthetic provider. Full conversation/memory/provider loop and error-shape parity remain |
| CLIENT-1 native consumers | White-label | macOS integrated `44d1fb7bd7`/`556dfc2f8c`: full app compile and actual named-bundle login, Keychain restore, protected JWT calls and logout/revocation passed against both targets; CF header WS used a synthetic ASR fixture. Android consumer is integrated (`44327be458`): both target debug APKs, real login/cold restore/refresh/Tasks/logout, rejected-password feedback and delayed owner/name/cleanup races passed; the CF name form was exercised offline and after recovery. Electron is now integrated (`11bd53bc21`) with macOS-hosted consumer evidence; iOS and actual Windows/Linux OS qualification remain |
| CF Python runtime | Cloudflare | Integrated `a1f4ae7733`: pinned workers-py/uv entry used by local startup and both publishers; 798 Worker tests, Core398, AI118 and seven dry-runs passed. Actual Core restart preserved task state. Native workerd launcher repair `1534fab379` preserves the runtime readiness descriptor; root independently passed its seven tests. Fresh tool installation remains registry-cache/TLS limited; version-checked installed runtime was exercised |
| CF-3 resource manifests | Cloudflare | Integrated `69929dcc98`/`701daa225d`: brand/stage/account input produces eight Worker configs and 28 resource entries, with migration/binding/secret-reference checks and output-ownership guards. Two real 26-route Web builds, 16 Worker dry-runs, four local D1 databases and brand isolation passed. Remote state remains unverified; release readiness is false |
| CF-4 pure/hybrid ownership | Cloudflare | Recording ownership and actual isolated runner integrated `d2c2ce3e9a`: D1 transcript/session owner, reconnect serialization, deleted/terminal fencing, existing Queue→Jobs→Core finalization. Fresh common HTTP8/8 + recording6/6 passed; Workers836/Core404/AI118, 12 upstream and 3 fork checks passed on the package candidate. Root independently passed 40 related tests. Controlled ASR/model RPC proves product state, not hosted inference. Explicit finalize is covered; continuous recording, remaining route ownership and complete erasure/export remain. Selected chat-session read/write/clear and conditional model commit are integrated (`0683918e46`), with actual Web core8/recording6/chat5 package evidence. Cache/app-history scope is integrated (`04baf36c50`). First-session admission is integrated (`40657792d7`): actual Core clear/delete during held model IO fences late replies; two first requests share one visible owner and quota remains bound to it. The Node22 process/cache fix is integrated (`931f63c716`); cleanup succeeds, and a package cold-cache run downloaded its dependencies. The root cold-cache run still timed out with an empty cache; explicitly reused verified cache passes actual core8/recording6/chat8. Manifest default-chat identity is integrated (`029aa3dd94`) with root actual core8/recording6/chat9. App-generation platform and goal-history persona prompts are integrated (`0b7848763c`), with package actual chat10; hosted model output quality is not claimed. Brand contact/presentation is integrated (`3d706b7812`). Branded public Chat and Tasks share links are integrated (`26914f62e7`): actual seven-Worker proof passes both share contracts, and root passes 15 Python and 19 Web share tests. `c29b84cfe5` repairs the deployed Worker path: workerd rejects transferable abort signals and `redirect: 'error'` on service-binding requests, so those controls remain only on external fetches. A rebuilt staged Web Worker connected to Edge returns task and chat previews with 200, reduced fields, and `private, no-store`; remote qualification and release readiness remain false. |
| CI-1 same product contracts | Integration | Common public identity/onboarding/Tasks suite and isolated Server runner integrated `0227847d28`; actual isolated seven-Worker CF runner integrated `d2c2ce3e9a`. Both target manifest lanes execute the same eight HTTP cases; CF also runs six separate recording cases. Native chunked ingress repair `ec9411593d` passed fresh Server HTTP8 + fixture4 and exact 13 upstream/3 fork checks. Earlier combined tree `f3e91e6dc4` passed 29 upstream/11 fork including Flutter and macOS compile. Electron package added its existing-workflow lane, with Windows/Linux runtime and repeated full macOS compile explicitly unproved. Integrated range `0c13c8dca1..c465df4e02` passes 24 upstream checks and five of eight selected fork checks, including a fresh Server image/HTTP8 and Electron two-target build. CF product startup failed on missing owned cache plus cleanup EPERM; AUTH failed on a Node22 builtin SQLite warning assertion; route checks were not reached. The later `0c13c8dca1..67851331b9` run passes 24 upstream and seven named fork checks with a verified explicit Pyodide cache, resolving the prior process/SQLite assertion failures; a fresh root cache timed out and remains recorded. `67851331b9..40657792d7` passes 24 upstream and five named fork checks including actual CF core8/recording6/chat8 and merged queue regressions. `40657792d7..106615b951` independently passes 12 upstream and all five diff-selected fork checks: actual CF core8/recording6/chat9, Electron two-target full builds, Web build contracts, routes and upstream touch. After v0.12.291 and the `6e53bbd2ec` / `09ff17e4e5` merges, the local seven-Worker contract passed core8/recording6/chat10/share; a fresh Server image passed fixture5 plus the same core8. Later app/goal and LegacyHome text commits retain package evidence and await the next combined run. No full-manifest, remote Actions or release qualification is claimed |
| WL-2 macOS identity | White-label | Isolated stage guards cover bundle storage namespace and disabled updater; complete signing/updater/package and supported-OS verification remain |
| WL-3 mobile identity | White-label | Android local debug package/profile/secure-storage staging is integrated `44327be458` with pinned Flutter3.44.5/Dart3.12.2. Upstream v0.12.291 removed the no-FCM notification implementation required by the fork stage; `194369a74c` makes that implementation a fork-owned overlay, so both self-hosted and Cloudflare stages again pass their 20-test suite and compile the real debug bundle; that result was repeated after the `6e53bbd2ec` and `09ff17e4e5` upstream merges. Real two-target native auth and CF name-error/recovery flows passed. The release task deliberately rejects this debug-only stage; iOS extensions, signing, push/OAuth, complete assets and distribution identity remain |
| WL-4 Windows/Linux identity | White-label | Electron identity integrated `11bd53bc21`: main-only opaque sessions, secure-store admission, serialized account cleanup, selected JWT/API relay and renderer ownership fences. Both target builds and actual macOS-hosted Electron login/refresh/cold restore/renderer Tasks/logout401 passed; root independently reran core12 + staged5. This is not Windows DPAPI/Linux keyring/installer proof. The production axios-header (`17fda545fc`) and graph-readiness (`c465df4e02`) fixes are integrated: actual complete onboarding, Tasks create/complete, cold restore, protected remote reread and settings logout passed on the CF test target. Full macOS compile was not repeated in this package because of local disk limits |
| WL-5 text/assets | White-label | Electron presentation (`67851331b9`), manifest PNG/ICO/renderer assets (`106615b951`) and actual old Home text (`f426815d2d`) are integrated. Real Harbor login/About/legacy Home/logout and six production WebGL frames pass; original animation/audio state owners stay intact. Two target staged builds and the root asset integration gates pass. `987d51b858` replaces native duplicated raster implementations with the shared, bounded PNG pipeline across Flutter, macOS, and Electron; its pinned Node 22 raster suite passes 2/2, with target staging and consumer checks recorded in the package evidence. This proves the listed Electron surfaces and staged native assets, not signed clients, iOS/Windows/Linux installers, a full visual scan, or every binary asset. CF default/app/goal prompts now consume the same manifest identity. The Server prompt package passes 15 behavioral/artifact tests, independent review and exact 24 upstream/3 named fork checks; actual Linux/model proof and integration remain |
| WL-6 device/OTA identity | White-label | Current local candidate generates one brand-scoped CV1 build/release policy, stages a copy of upstream `omi.conf`/NFC source with BLE/DIS/NFC identity, bakes the policy into Server OS images, and projects it into Cloudflare `api-core`. Server OS uses only its configured operator manifest; Cloudflare fails closed without the policy and filters models, tags and OTA assets by that policy. Stage/Server router/Cloudflare policy and resource-plan fixtures pass. Signing-key resolution, NCS/MCUboot release builds, remote publishing and hardware OTA evidence remain release gates |
| WL-7 artifact matrix | White-label | Local synthetic-brand matrix now stages, behavior-tests and builds both `self_hosted` and `cloudflare` targets for Android/Flutter, macOS and Electron. The macOS matrix emits separate named debug binaries and deletes partial output on a target failure; Electron source-owner review now covers the upstream Settings authentication import. This is compile-only/debug verification, not an iOS/Windows/Linux native package, signing/notarization, store release, remote deployment, hardware, or release qualification; see [WL-7 evidence](implementation-2026-09-05/WL7-local-artifact-matrix-verification.md). |
| SH-4 Linux operations/release | Server | Stage-owned Auth launcher and dependency-cache/source-label package integrated `85e6e8ebc1`; local startup/migrate and beta/prod rejection passed. Runtime context cache leakage is repaired (`96380cfcb8`): both actual Docker context filters and a standard amd64 image exclude local validation state. `aef38a8800` scopes the bounded identity/onboarding/Tasks contract to the controlled runtimes it actually starts, disabling unowned speech and local LLM capabilities. The source-byte-admitted runtime image then passed all five runner checks, normal migrations, and core8 over an isolated Docker project. Full Linux install/upgrade/restore/model acceptance and candidate manifest remain |
| CF-5 staging release | Cloudflare | Local frozen candidate tools are integrated (`bc64267eb9`/`ff3610160d`): Moonshine dual builds, eight Worker payloads, hashed source/assets, ordered resource/version journal, bounded subprocess ownership and conservative recovery. Actual local prepare/check/build/dry-runs passed. Apply/restore refuse missing CF-4/CI-1/prior-schema proof; no remote resource creation, secrets, migrations or publishing has been executed |

Engineering fixtures use synthetic brand names and temporary local data. Actual
distribution identities, signing/OTA keys and hardware results must be recorded
when available; they cannot be inferred from fixture builds. Access-control,
migration and release changes require explicit authorization before merging or
remote deployment under repository rules.

Recent evidence: [dual-target Web Tasks](implementation-2026-09-04/WEB-tasks-verification.md), [PG auth migration](implementation-2026-09-04/AUTH1-pg-migration-verification.md), [selected identity deletion](implementation-2026-09-04/AUTH-identity-deletion-verification.md), [import ordering](implementation-2026-09-04/AUTH-import-order-verification.md), [provider erasure](implementation-2026-09-04/SH2-providers-verification.md), [real model/runtime](implementation-2026-09-04/SH3-model-verification.md), [stage-owned startup](implementation-2026-09-04/SH4-startup-verification.md), [CF realtime](11-cf2-realtime-evidence.md), [CF Python runtime](12-cf-runtime-evidence.md), [CF resources](13-cf-resource-evidence.md), [CF frozen release](14-cf-release-evidence.md), [PG onboarding v5](implementation-2026-09-04/SH2-onboarding-schema-verification.md), [PG v6 canonical memory](implementation-2026-09-05/SH3-pg-v6-verification.md), [upstream v0.12.291 merge](implementation-2026-09-05/upstream-sync-v0.12.291-verification.md), [Flutter upstream compatibility](implementation-2026-09-05/flutter-upstream-sync-verification.md), [latest upstream sync](implementation-2026-09-05/upstream-sync-6e53bbd2-verification.md), [upstream v0.12.292 merge](implementation-2026-09-05/upstream-sync-v0.12.292-verification.md), [Docker context](implementation-2026-09-04/SH4-build-context-verification.md), [speech](implementation-2026-09-04/SH3-speech-verification.md), [PTT terminal usage](implementation-2026-09-04/SH3-ptt-usage-verification.md), [Android consumer](../../app/fork/VERIFICATION.md), [macOS consumer and limits](../../desktop/macos/fork/README.md), [Electron consumer and limits](../../desktop/windows/fork/VERIFICATION.md), [CF recording](implementation-2026-09-04/CF4-recording-product-verification.md), [PG nested usage](implementation-2026-09-04/PG-nested-usage-verification.md), [selected identity profiles](implementation-2026-09-04/AUTH-profile-verification.md), [integrated c465 checks](implementation-2026-09-04/CI1-integrated-c465-verification.md), [integrated 406 follow-up checks](implementation-2026-09-04/CI1-integrated-406-verification.md), [brand integration checks](implementation-2026-09-04/CI1-brand-integration-verification.md), [public share binding](implementation-2026-09-05/CF-public-share-binding-verification.md), [white-label local artifact matrix](implementation-2026-09-05/WL7-local-artifact-matrix-verification.md).

Evidence is per package and exact local candidate tree. The Android/speech
combined check range is `96380cfcb8..f3e91e6dc4`; it is not a full cumulative
release-matrix run. Shallow-history recurrence checks explicitly reported SKIP.
The common-core candidate tree `bdbf05522abc51cedf8d29bd2f7a6fa640d96b7b`
passed its selected gates against `1534fab379`; see the
[core product evidence](implementation-2026-09-04/CI1-core-product-verification.md).
Stopped owned test services and removed compile-only caches are not deployed
products; logs and source records remain under `/tmp/memweft-implementation`.
Successful Tasks, identity and vector checks do not qualify every API, a complete
recording-to-memory loop, all white-label surfaces, signing or supported OSes.
