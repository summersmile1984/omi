# Server 产品入口：真实长响应转发

2026-09-06，基于 `43eb4388bd` 修复验收入口的 HTTP 转发。
录音后的聊天在 API 已返回 200、容器仍 healthy 时断开；入口原先以
30 秒上游 socket 超时读完整响应后才发送，真实 CPU 模型超过该等待时间。

`deploy/self-host/ci/loopback.py` 现在收到响应头就发送，使用分块编码逐段
转发真实上游响应。总量 64 MiB、总时间 900 秒，客户端写入仍有 30 秒超时。
上游中断或超过预算时不发送正常结束块，客户端可以识别响应未完成。
该入口只用于隔离验收环境，不更改生产网关或模型输出。

现有 runner 的真实 TCP 回归用例要求：客户端收到首个 SSE 事件后，才允许
上游发送结束事件。因此先缓冲到 EOF 的旧实现无法通过。用例同时覆盖上游
截断不能变成正常完成；连同 HTTP framing、WebSocket、生命周期和容量检查，
9 项通过。

使用实际 Qwen/BGE-M3 的 d 轮服务，HTTP 200 和首个进度分别在约 0.18 秒内
到达；约 249.74 秒收到 `done:` 结束帧，连接保持正常。
但答案为没有找到饮品信息，未包含已保存的 jasmine 偏好，也没有个人历史
检索进度事件。因此本次只证明长流传输已恢复，**没有通过检索业务验收**。

私有证据：`server-tokenizer-retrieval-20260906-b.log` 及 d 轮
`speech-probe/retrieval-after-streaming.txt`。账户和录音均为工程合成材料。
仍需解决实际检索路径、导出及完整 CI-1/CF-4 后才能发布 Eddy 生产。
