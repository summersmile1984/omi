# Local dev env (contains dev-only secrets) — fork-owned, keeps the root .gitignore untouched
.env.dev
# machine-local port allocation for dev/local.sh (committed example: local.env.example)
local.env
