# Cloudflare 适配架构与完成度审计

本目录保存 Cloudflare Workers 适配的架构视图，以及截至 2026-09-01 的完成度审计。结论先行：独立的 Cloudflare production 空数据新部署已上线并通过鉴权、Workers AI、性能、浏览器和删号残留验证；它使用专属 `*-production` 资源与 `workers.dev` 域名，没有合流其他适配分支，也没有修改 `api.omi.me` / `app.omi.me`。可选业务集成凭据、正式域名 rollout 和旧基础设施清理仍需单独发布门槛；生产数据回填与旧协议兼容不在本期范围。Workers AI 主路径不需要 OpenAI/Gemini secret。

## 架构图

完整的可交互、响应式和可打印版本见
[cloudflare-production-architecture.html](./cloudflare-production-architecture.html)。

| 图                                                                 | 内容                                                                      |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| [06-production-overview-gpt-image.png](./06-production-overview-gpt-image.png) | 最新独立 Cloudflare production 总体拓扑                                   |
| [07-request-auth-route-gpt-image.png](./07-request-auth-route-gpt-image.png) | 请求、Better Auth、账户切换、限流与 Worker 路由链路                       |
| [08-data-async-lifecycle-gpt-image.png](./08-data-async-lifecycle-gpt-image.png) | D1/R2/Vectorize/Queues/Jobs 数据流、搜索投影与删号生命周期                 |
| [09-production-isolation-release-gpt-image.png](./09-production-isolation-release-gpt-image.png) | Legacy、staging、production 隔离，以及发布与回滚顺序                      |
| [01-overview-current-target.png](./01-overview-current-target.png) | 当前 staging、迁移窗口和稳定目标架构                                      |
| [02-request-auth-flow.png](./02-request-auth-flow.png)             | HTTP、WebSocket、异步任务和认证边界                                       |
| [03-data-async-plane.png](./03-data-async-plane.png)               | D1、R2、DO、Queues/DLQ、Workers AI、外部 API 与 Vectorize 的数据/异步平面 |
| [04-migration-roadmap.png](./04-migration-roadmap.png)             | CF-00～CF-13 路线图及完成度                                               |
| [05-route-ownership.png](./05-route-ownership.png)                 | 当前已声明路由的 Worker owner 和迁移窗口兜底                              |

剩余 legacy 路由的逐组依赖、阻塞条件和迁移顺序见 [legacy-route-audit.md](./legacy-route-audit.md)。

## 本期验收范围

本期目标是新 Cloudflare 部署：使用 Better Auth、D1/R2/Queues 和 Cloudflare
Workers AI，从空数据集开始承载新的 Web/Worker 客户端。Calendar、Twilio 等
明确需要的业务集成仍可通过各自 REST API 接入；OpenAI/Gemini 不属于本期的
生成式 AI 依赖，相关兼容适配器只能作为显式可选面。Firestore/GCS 历史回填、
Firebase legacy token、旧 Cloud Tasks/Assistants wire 和旧客户端逐字节兼容不
属于本期完成条件；这些能力保留为后续迁移窗口。当前必须闭合新客户端实际使用
的 Worker owner、Workers AI 调用、删除残留、staging live smoke、部署凭据和
回滚证据。Web app 表单的通知 scope 元数据调用使用
`/v1/app/proactive-notification-scopes`（单数 `app`），与 Edge/API Core
canonical route 保持一致。

## CF-00～CF-13 完成度

| 项目                       | 状态                    | 已有证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 尚未闭合                                                                                                                                                              |
| -------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CF-00 清单/scaffold        | 已完成                  | hermetic FastAPI 注册表确认 577 条唯一后端路由（573 HTTP、4 个 WebSocket，含显式 HEAD/OPTIONS）；577 条已匹配 Cloudflare staging owner，0 条保留 legacy owner；627 条 Cloudflare 路由、24 个隔离资源、34 个 Redis family、7 个 vector namespace、9 个 R2 namespace 均有机械校验                                                                                                                                                                                                                                            | 路由 owner 已闭合；历史数据、provider 和生产切换门槛仍归入 CF-10～CF-13                                                                                               |
| CF-01 Edge                 | staging 已验证          | header 剥离、防重放、CORS、owner 标记、health/smoke                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 生产回滚与非幂等超时的 live 证据                                                                                                                                      |
| CF-02 Python Workers       | staging 已验证          | api-core/api-ai、388+116 个 Python 测试、真实 Worker smoke；API AI 已验证跨 Worker DO binding                                                                                                                                                                                                                                                                                                                                                                                                                              | 生产规模 bundle/CPU/内存/cold-start/外连预算                                                                                                                          |
| CF-03 Better Auth          | 部分完成                | 独立 D1、signup/login/session/JWT、staging 浏览器登录；MCP OAuth PKCE、discovery、token verification、grant list/revoke 和 Streamable HTTP/SSE transport；生产默认关闭匿名 DCR；内部身份查询/原子删除/residual；Better Auth-only 隔离 staging 账号的 Edge→Jobs→Auth 删除链路已通过 live Queue、双 residual scan、产品清理、身份清理与 tombstone 验证；Firebase password verifier、首次登录原生 rehash、参数化 D1 import ledger/checksum/replay tooling；真实 workerd+D1 已验证错误密码不写、成功升级和移除迁移密钥后再登录 | CIMD 安全 metadata-fetch boundary、真实 Google/Apple OAuth/link、Stripe provider cleanup、真实 Firebase export 导入演练、JWKS retired/grace live 证据和生产身份连续性 |
| CF-04 R2                   | staging 已验证          | PUT/GET/range/conditional/checksum/delete smoke；`0145_asset_multipart_uploads.sql` 与 API Core 的 R2 `createMultipartUpload`/`resumeMultipartUpload`/part ledger/complete/abort`，uid 隔离、幂等、校验和、删号 fence 本地回归；2026-09-01 authenticated staging multipart 上传/下载成功（5 MiB+尾部、SHA-256 校验）并随后删号                                                                                                                                                                                             | presigned expiry（当前没有启用 direct-to-R2 client）、中断恢复 live 演练；生产 rollout 仍另行处理                                                                     |
| CF-05 App D1               | 选定 route group 已验证 | 用户设置、conversation、memory、recap/chat 等投影路由                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | provider/正式域名 rollout；历史数据回填与账户级旧 authority cutover 不在本期                                                                                          |
| CF-06 Queues/Workflows     | 部分完成                | transcription Queue、幂等/重复/冲突/终态、DLQ consumer/index/replay boundary；账号删除已有 durable D1 intent、intent/tombstone mutation fence、无 uid Queue payload、分批 D1/R2 purge、双 residual scan、Jobs→Auth finalization 与 Queue-send/Auth-failure recovery 契约，并已通过 live staging Queue 完整执行；conversation finalization/reprocess/merge 的 D1 lease/retry/processor 契约已实现并有回归测试，merge 已完成 staging live 验证                                                                               | Stripe provider cleanup、历史 DLQ backlog 控制面导出、DLQ consumer/replay staging live 验证                                                                           |
| CF-07 Redis primitive 拆分 | 已完成（清单闭合）      | staging 新 Worker 不接 Redis；通用限流路由/policy 与桌面 TTS 细粒度额度在独立 rate-limit Durable Object；2026-09-02 对全部 21 个 planned family 与 16 个 planned direct caller 完成实现审计：11 个 family 已由既有 D1/DO 权威覆盖、6 个在 Worker 架构下已过时（retired）、3 个真实缺口已实现闭合——realtime 每 uid 会话准入限流、跨 app 共享的每用户每日通知上限（cf_user_notification_daily_usage）、daily-summary regenerate 冷却 policy；另补 persona username 的 D1 部分唯一索引；manifest 逐项校真并附证据注释      | 仅余两项 planned：developer webhook 的健康评分/自动禁用；Workers AI 翻译路径的缓存（成本优化）                                                                        |
| CF-08 Realtime + ASR       | 部分完成                | Realtime DO 协议/WebSocket、Workers AI ASR/STT/TTS/翻译 smoke                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 多语言/噪声 WER、首字/final p50/p95/p99、重连、成本、区域与 cohort                                                                                                    |
| CF-09 Vectorize            | 部分完成                | memory、action-item、conversation summary、transcript chunk 四个多语言 BGE-M3/1024 维 staging index；D1 authoritative hydrate、hashed namespace、outbox/Queue/cron repair、删除清理、semantic recall 和 UTC 日期 filter 已 live 验证                                                                                                                                                                                                                                                                                       | 正式域名 rollout 与质量/成本基线；生产数据全量 backfill 不在本期，workstream 与 3072 维 screenshot embedding 另行规划                                                 |
| CF-10 其余 API 领域        | 部分完成                | 已迁移并声明 owner 的 core/ai 路由；当前 route inventory 已无 `legacy-owned`                                                                                                                                                                                                                                                                                                                                                                                                                                               | 仅对明确启用的 Calendar/Twilio 等业务集成做 provider smoke；正式域名 rollout；历史 Firebase/Redis authority 和旧协议 parity 不在本期                                  |
| CF-11 Web/vinext           | staging 已验证          | Web Worker build/deploy、login/recaps/chat/conversations 等浏览器路径                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 生产域名、生产身份、Firebase 连续性和完整 E2E 门槛                                                                                                                    |
| CF-12 生产数据迁移         | 本期不适用              | 本期从空数据集启动，没有 Firestore/GCS 生产数据需要回填                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 若未来导入历史数据，再单独建立 snapshot/checksum、回放、备份恢复和 rollback 证据                                                                                      |
| CF-13 生产切换/清理        | 本期不适用              | 本期交付 Cloudflare staging/新部署，不执行旧后端清理或生产流量 rollout                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 若未来切生产，再单独建立 rollout、on-call/SLO/cost 和 legacy cleanup 计划                                                                                             |

## 本轮验证证据

- 独立 Cloudflare production（2026-09-01）：Auth/App D1 分别完成 10/150 个 migration 且无待应用项；Auth `502fa91c`、Rate Limit `abdf358c`、API Core `fd80438f`、API AI `bb938ec8`、Realtime `569055c6`、Jobs `63d79cb4`、Edge `d25adeca`、Web `d5321b10` 均为 100% active。Edge/Web readiness 与所有内部依赖均为 200；完整 authenticated smoke（含一次真实 Workers AI chat）通过。六端点各 20 次暖路径基准全部低于 4,000 ms p95，最大 p95 为 1,847.97 ms。真实浏览器完成登录并访问 Conversations/Memories/Apps/Tasks，187 个 CDP 事件中无 console exception、network failure 或 HTTP 4xx/5xx。一次性账号经公开 Queue 删号后，Auth user/session/account、App intent/cutover/chat residual 均为 0，只保留预期短期 tombstone，Bearer 失效为 401，浏览器回到 `/login`。现有 `api.omi.me` / `app.omi.me` 未切换。
- Cloudflare TypeScript：100 个测试文件、730 个测试通过；类型检查和生产依赖审计通过。
- `api-core`：388 个测试通过；`api-ai`：116 个测试通过。
- 2026-09-02 缺口闭合批次：R2 manifest 校真（temporal-sync/app-logos/app-thumbnails 记录为共享 ASSETS 前缀替代实现）并为校验器补 active-需有已供给资源/真实 binding/共享 binding 前缀不相交三条 ratchet；realtime 会话准入限流（realtime:session_admission，10/60s/uid，防 x-omi-session-id 变造导致的无界并发 STT 流）；每用户每日通知上限（默认 9/日、开发者豁免、跨 app 共享）；daily-summary regenerate 冷却（2/60s，edge policy）；persona username D1 部分唯一索引 + 409 映射；级联会话删除 Worker 化（同批原子撤回派生 memory/action item + vector 撤回 outbox + 幂等 conversation_audio_purge 队列任务清理逐会话 R2 音频）。全量回归：Cloudflare vitest 763 通过、api-core pytest 391 通过、typecheck 与 manifest 校验通过。
- Web：10 个测试文件、43 个测试通过；vinext staging build 与 Worker dry-run 通过。
- 当前分支执行 `npm run smoke:staging` 的 edge/v1 health、公开页面和安全边界检查均通过；本轮使用一次性 Better Auth 账号启用了 authenticated checks，Edge/Web health 均为 200，Workers AI chat、conversation、calendar onboarding/status 等路径通过。账号已提交删除，删除后 token 访问返回 404。
- 2026-09-01 authenticated R2 multipart probe：创建 multipart、上传 5 MiB 非末尾 part 与尾部 part、按 D1 ledger 完成并校验 SHA-256，随后 GET 返回 200/5,242,895 bytes；一次性账号已提交删除。该探针不需要 OpenAI/Gemini secret，也不启用 presigned direct-to-R2。
- 当前提交 `49633dab56` 已发布 staging：Auth `529c7b31-bfbb-4f47-816b-966fbfe420e0`、Rate Limit `108781af-129d-4c16-84c8-63245320dcd7`、API Core `1d446b76-91ba-4883-a546-ffe9b3c4f4cf`、API AI `ddf5c5a7-bbf2-4ff8-9033-d117f85b1593`、Realtime `f4f4095e-c91a-436c-a78b-179e4e1a6264`、Jobs `2cf2b435-42f9-4467-bef1-0623df0a2ad3`、Edge `0a72dcab-d6f0-4ec8-9bca-24b6ca9f4d20`、Web `6b7f2456-1d8d-4f8f-abe2-2120f55a2f14`。远端 APP_DB 已应用 `0137`–`0145`，Edge/Web health=200，authenticated smoke 通过；本次发布不依赖 OpenAI/Gemini secret。
- Rate Limit Durable Object 已增加内部 `/reserve`/`/release` 原子配额预留基础能力，覆盖并发、幂等释放、窗口过期清理和 stale-token 隔离；当前不切换 Redis family 或 legacy caller，后续需按调用方契约逐组迁移。
- 历史回放输入边界（2026-09-01）：通用 `backfill-d1` 输入上限为 64 MiB，并以 fatal UTF-8 解码；Chat/Files、Memory、Persona、Phone、Wrapped、Audio 七个 reconcile 工具同样拒绝 malformed UTF-8，避免损坏 export 进入迁移计划。该保护不等于已完成真实 Firestore 回放。
- Calendar provider probe（2026-09-01）：新增显式确认的 staging-only `calendar:positive-probe`，使用一次性 Bearer/access token 验证加密 grant 保存、Google events 读取和自动断开，不创建事件、不输出 token；当前因缺少 Google OAuth client secret 尚未执行真实探针。
- manifest：627 条 Cloudflare 路由、577 条完整 backend 路由和 24 个 staging 资源通过校验；0 条 legacy-owned 路由。D1 review queue 的三个端点已由 API Core/Edge 承载，并由 canonical memory 写入 producer、source revision/hash projection 和原子 resolve 覆盖；conversation finalize/status/reprocess/merge 已声明 API Core owner，`0094`/`0095`/`0096` staging migration 与 live Queue 验证已完成；`GET/POST /v1/agent/*` 已切到 API Core，并仅声明/执行已具备 D1 authority 的一方工具。Web 登出所需的 `DELETE /v1/users/fcm-token` 也已补齐 API Core/Edge/D1 删除边界，避免注销时残留 token 导致 404。
- Queue DLQ replay staging seam（本轮）：Jobs 作为 DLQ consumer 将有界、hash-bound 的 `JobMessage` envelope 索引到 D1；`POST /internal/cf/jobs/dlq/replay` 需要 `ADMIN_KEY`、5 分钟 HMAC timestamp/signature、content-bound idempotency key 和最多 50 个 message id，只能重新发布已捕获消息，不能提交任意 payload。`sync_local_files` 按已校验 lane 返回对应 Queue；默认 `DLQ_REPLAY_STAGING_ENABLED=false`。新增 `npm run dlq:positive-probe` 作为 staging-only 正向探针：它只接受已捕获 message id，要求成功的单消息 `202`/幂等 `200` receipt，并拒绝 unknown-id/partial 结果；不会创建 DLQ 消息或把异步 replay 误报为完成。Cloudflare Worker 无法读取历史 DLQ backlog，因此部署 consumer 前的 DLQ 消息仍需控制面导出。
- 本轮新增两个正向 owner：Wrapped 的两条 `/v1/wrapped/*` 路径由 Jobs 以 `0107_wrapped_jobs.sql`、D1 bounded projection、Workers AI structured output、Queue lease/retry 和 notification outbox 承载；六条 `/v1/phone/*` 路径由 Jobs 以 `0108_phone_twilio.sql`、Twilio REST/Voice JWT、签名 TwiML、quota 与 deletion fence 承载。两组均已通过本地正向/失败/跨账号测试；真实 staging provider probe、历史 Firestore backfill 和 production cutover 尚未完成。
- 新增 owner 的 staging 发布证据（2026-08-31）：远端 App D1 已应用 `0107_wrapped_jobs.sql` 与 `0108_phone_twilio.sql`；API AI `e0931245-069a-4e4d-a689-9a0abbad39e0`、Jobs `16b22381-cff7-42e4-a8d1-f01a61067fcd`、Edge `356ddb07-fd56-486c-b1f1-5773dddd2757` 已发布。未认证请求对 Wrapped、Phone、`/v2/cf/chat/completions` 与 audio merge 均返回稳定 `401 unauthorized`；未提供 Twilio/AI fixture，因此尚未宣称 authenticated positive provider/Queue drain。
- `/metrics` staging boundary 已接入 Edge → API Core：Edge 仅转发 operational Bearer，API Core 在 `METRICS_SECRET` 缺失或 Prometheus authority 尚未建立时返回明确的 `503 metrics_unavailable`，错误凭据返回 `401`，不生成空或历史指标文本；真实聚合、保留和 scrape authority 仍待定义。
- Memory admin non-active report 已接入 API Core/Edge 的 staging-only D1 projection（migration `0100_memory_admin_report.sql`）；未提供 admin key 返回 `403`，缺少 completed cutover 或 deletion fence 时 fail-closed。短期 lifecycle runner 已使用 `0104_memory_lifecycle_projection.sql` 切到 Jobs staging owner；远端 migration 已应用，未认证边界已实测，带 admin key 的正向/重试/删号 fixture 仍需补充。
- Chat files staging boundary 已发布 canonical `/v1/cf/chat-files`：文件只写 uid-scoped `CHAT_FILES` R2/D1，文本类文件问答由 Workers AI Queue 执行，不需要 OpenAI/Gemini secret；配置 `IMAGES` 后图片缩略图通过 HMAC URL 读取。旧 `/v1/files`、`/v2/files` aliases 默认关闭，旧 Assistants continuity、历史 Firestore/GCS 回放属于明确排除的兼容窗口。
- Chat session attachment projection staging evidence（2026-08-31）：远端 App D1 已应用 `0111_chat_session_files.sql`；最新 API Core `23e7629e-1725-43fd-a17d-1c24c1d340aa`、Jobs `48f4ed33-79ff-49d0-8abc-d85c62abcbd0`、Edge `ee262b9c-0d14-42c5-a0b0-c46e0ffc0a69` 已发布。隔离 Better Auth 账号的 session/file reader、跨 uid/缺失 canonical file rejection 均已有 staging 验证；API Core `POST /v2/desktop/messages` 现在只接受 ready canonical `file_ids`，在同一 D1 batch 写入 `cf_chat_session_files` 并固化安全 metadata projection；不存在的 session+attachment 在 mutation 前稳定返回 `404`。该 projection 只证明 uid/session/ready/deletion fence，不承接 `/v2/messages` 的 Assistants thread/file_search/run；随后 `/v1/files`、`/v2/files` 已切到 Jobs staging owner，历史 Assistants continuity 仍未完成。
- Chat attachment bridge staging evidence（2026-09-01）：`/v2/messages` 的附件分支（含 app/context）在 Edge 经过 Better Auth signed context 转发到 Jobs `/v2/cf/messages/attachments`，由 `cf_chat_session_files`、Workers AI run/message projection 与 Queue 承载；首次 admission 为 `202` + polling `Location`，Web 客户端已实现轮询，普通文本仍走 API-AI Workers AI。Jobs 会按 app-scoped session 校验 D1 app projection，并把已 ready 的 canonical file id 关联到新会话；文本内容以有界、不可信参考数据注入 Workers AI prompt。该 native boundary 不读取 `OPENAI_API_KEY`/`OPENAI_ASSISTANT_ID`；旧 envelope/Assistants adapter 仅保留为默认关闭的兼容代码。
- Workers AI native chat-files 发布证据（2026-09-01）：提交 `b154b928a5`/`5115514491` 后，远端 App D1 已应用 `0146_chat_files_workers_ai.sql` 与 `0147_chat_runs_workers_ai.sql`；Jobs `d64a4156-289f-4f27-bce0-ff40bdad199f`、API AI `325e0d60-da8d-4c64-a721-348b67e46079`、Edge `8af18a65-2c27-4a69-aac7-f7b1fb54d223` 均已发布到 100%。Workers AI 是 canonical 文件问答 provider，外部 OpenAI/Gemini secret 不在部署依赖内；Worker 全量 100 files/732 tests、Web 11 files/44 tests、manifest 627/577/0 通过，公开 staging smoke 通过（authenticated smoke 需独立 token）。
- Workers AI provider policy recheck（2026-09-01）：API-AI staging 已重新发布为 `0b1a3325-1bdf-4107-8574-b2b954f8c8f9`，移除 `EMBEDDING_MODEL`、`BYOK_OPENAI_CHAT_MODEL`、`GEMINI_PROXY_PROVIDER` 和 `GEMINI_DAILY_LIMIT` 等外部 AI 模型变量，仅保留 `GEMINI_PROXY_ENABLED=false` 的 fail-closed 开关；Web staging 已更新为 `e530c421-ebfa-4e78-adc9-cf8df6569fd5`。Workers AI chat/file-Q&A/ASR/translation/TTS/embedding 路径不读取 OpenAI/Gemini secret，公开 smoke 继续通过。
- Embedding native owner recheck（2026-09-01）：`POST /v1/embeddings` 与 `/v1/embeddings-workers-ai` 现在共用 `@cf/baai/bge-base-en-v1.5` Workers AI 执行器，保留 OpenAI-style envelope 但不读取外部 embedding secret；API-AI `26b44074-683d-4a6f-bb8c-40540967584f` 已上传 staging。API-AI 全量 116 tests、Cloudflare Worker 全量 100 files/732 tests、typecheck、manifest（627/577/0）和公开 smoke 通过。
- Chat-file historical reconciliation staging slice（2026-09-01）：`0119_chat_file_import_ledger.sql` 与 `scripts/chat-file-reconcile.mjs` 提供默认 dry-run 的 Firestore/GCS metadata 规划、checksum/provider-id/R2 key 校验、重复冲突阻断、输入/行数/文件大小上限和 account-deletion fence；不会在缺少真实对象 checksum/provider 验证时生成 ready `cf_chat_files`，也不宣称已完成 GCS→R2 copy 或历史 owner 切换。
- Chat-file reviewed history apply staging（2026-09-01）：远端 App D1 已应用 `0137_chat_file_history_executor.sql`，Jobs `284b0d1c-caaa-43fa-b438-9f8d08e660f0` 已发布。Jobs-direct endpoint 默认 `CHAT_FILE_HISTORY_IMPORT_STAGING_ENABLED=false`，仅接受 content-bound plan、provider-object HMAC attestation、`CHAT_FILES.head()` checksum/size、account-generation/cutover 与 deletion-fence 全部通过的条目；它刻意不进入 Edge/manifest，不代表真实 Firestore/GCS 回放、OpenAI provider continuity 或 production cutover。
- 最新 staging 发布证据（2026-09-01）：Auth `04d87a18-4254-427b-a33b-9dce7d22dd6a`、Rate Limit `766d0b2a-f636-4c86-a119-c93d5636dd75`、API Core `c00b59dc-aff6-4f6a-bf0d-2c68881ccd46`、API AI `30414054-5ee6-45cc-a127-4163f86f8646`、Realtime `a6987a2a-7305-4b44-ac31-2737051320f1`、Jobs `1ad49938-1527-4197-bb5a-936949c89a75`、Edge `88e07384-db7f-4fce-a305-bad9100ec89f` 与 Web `22def64c-3afb-46c1-b8c5-890d623ae2ba` 均已发布；远端 D1 无待应用 migration，自动 authenticated smoke 全部通过，Edge/Web health 均为 `200`。新 disposable 账号探针确认 `/v2/chat/completions` 返回 `200` Workers AI，`/v1/files` 与 `/v2/files` 的非 multipart 请求由 Jobs 返回 `400`，合法 Gemini 模型在未配置 provider secret 时返回 `503 gemini_provider_unavailable`，exact Auth/OAuth 在缺少 provider/Firebase 配置时按设计 `503` 且 `no-store`。账号删号流程未在本次 smoke 中执行，避免把发布凭据误删。
- MCP exact owner live probe（2026-09-01）：Jobs `ac14b2c1-b03c-4f46-8cee-66f315243916`、Edge `9ab249de-8153-45bd-99ad-be1ec6ccdf2c` 发布后，补齐 staging `MCP_APP_TOKEN_ENCRYPTION_SECRET`；Better Auth disposable 账号调用 `POST /v1/apps/mcp` 无效体返回 `422 invalid_app_name`，`GET /v1/apps/mcp/callback?state=bad&code=bad` 返回 HTML `400`，`POST /v1/apps/no-such-app/mcp/refresh` 返回 `404 app_not_found`。该账号删号完成后远端 D1 `cf_account_deletion_intents=0`、`cf_mcp_app_connections=0`、`cf_user_enabled_apps=0`，仅保留 1 条 tombstone。真实 MCP provider replay 与 Firebase/catalog 历史回放仍未完成。
- Remaining legacy boundary live probe（2026-09-01）：exact `/v1/auth/*` 与 `/v1/oauth/*` 已由 Auth/Jobs staging owner 承载；缺少 Google/Apple/Firebase 配置时返回 `503` 且 `cache-control: no-store`，不调用 legacy。Twitter ownership exact owner 已接入 manifest/Edge→Jobs，但默认 gate/RapidAPI secret 关闭时返回 no-store `503`；当时 `POST /v1/apps/migrate-owner` 仍返回 `503 persona_apps_unavailable`。随后 app-owner exact adapter 已接入并发布，当前 manifest 为 0 条 legacy-owned；Firebase identity/catalog backfill、owner-migration side effects、旧客户端 conformance 与 production provider parity 仍未完成。
- Persona/apps staging seam release（2026-09-01）：App D1 `0121_twitter_ownership.sql`、`0122_app_owner_migration.sql` 已应用；Twitter exact owner 已接入 Jobs/Edge manifest，`TWITTER_OWNERSHIP_STAGING_ENABLED`、`TWITTER_OWNERSHIP_EXACT_STAGING_ENABLED`、`APP_OWNER_MIGRATION_STAGING_ENABLED` 和 executor gate 默认均为 `false`。关闭时 exact Twitter 返回 no-store `503`，namespaced seam 保持关闭；开启且注入 RapidAPI secret 后，隔离 fixture 可验证 timeline/profile → D1 Persona metadata projection。该证据不代表 Firebase/Firestore、prompt/memory/cache parity 或 production cutover 已完成。
- Persona/apps seam Edge recheck（2026-09-01）：Edge 已接入 Twitter namespaced、Twitter exact owner 与 app-owner migration/status 路径；Twitter exact gate=false 时返回 no-store `503 twitter_ownership_unavailable`，owner-migration seam/status 仍由独立 gate 保护。请求中的 bearer/cookie 均未转发给 Jobs 之外的 legacy。
- Assistants/Gemini staging adapter 发布证据（2026-08-31）：远端 App D1 已应用 `0113_chat_assistant_provider.sql` 与 `0114_gemini_proxy.sql`；API-AI `15a48911-bc8a-41f1-bd40-7a671f5d24e5`、Jobs `969c6d84-1cd3-4c8f-8781-914c44366349`、Edge `261cf422-4a28-4841-b316-cea5711f9d7a` 已发布。Worker 全量 67 个测试文件/512 个测试通过，API-AI 100 个测试通过；匿名 Assistant 入口为 `401`，Better Auth 临时账号在功能开关关闭时返回 `404 legacy_route_disabled`，Gemini legacy 入口仍为 `503 gemini_proxy_unavailable`。两个 adapter 都保持显式 staging opt-in，缺少 `OPENAI_*`/`GEMINI_API_KEY` 时 fail-closed，未切换 `/v1/files`、`/v2/files` 或 Gemini manifest owner；Vertex/Firebase/旧客户端 wire parity 与真实 provider positive probe 仍未完成。
- Firebase identity bridge staging evidence（2026-08-31）：Auth D1 远端已应用 `0007_legacy_compatibility_authority.sql` 与 `0008_firebase_custom_token_bridge.sql`；Auth Worker `b163f32d-1eb0-460c-89b9-56d7cc0766fd` 已发布，`/health` 返回 `200`（`cf-04`）。由于 staging gate 保持 `false` 且未配置 service-account/API key，`POST /internal/firebase/custom-token` 按设计返回 `404 not_found`；Worker 测试 67 个文件/512 个测试、API-AI 100 个测试、typecheck、backend-route 与 manifest 校验均通过。该 namespaced seam 只证明导入 projection、generation/deletion fence 和 fail-closed 边界，不承接 `/v1/auth/token` owner；旧 Redis authorize/callback/PKCE、Firebase 正向 exchange、旧客户端 wire parity 和 production identity cutover 仍未完成。
- Native auth replay seam staging evidence（2026-08-31）：Auth D1 已应用 `0009_legacy_auth_transaction_metadata.sql`，Auth Worker `6a870960-a909-41e9-a216-258c6cb5589a` 已发布。该 seam 覆盖加密 session metadata、Google GET/Apple form-post callback、provider mock exchange、hash-only code、S256 PKCE、单次消费、过期/重放和 provider failure；随后 exact `/v1/auth/*` 已切到 Auth staging owner。真实 Google/Apple/Firebase 正向兼容仍未宣称。
- External App OAuth staging owner evidence（2026-09-01）：App D1 `0116_external_app_oauth.sql`、Jobs handler 与 Edge/manifest wiring 已提交；`/v2/cf/oauth/*` 与精确 `/v1/oauth/*` 共用 Better Auth uid、hash-only state/CSRF、private/paid/tester/setup admission、install CAS 和 deletion fence。focused Auth/OAuth/Edge 149 tests、全量 Worker 72/574、typecheck 和 manifest（614 routes）通过；无 Firebase/provider secrets 时精确 `/v1/oauth/*` 仍按设计 fail-closed，未宣称历史 catalog 或旧客户端 wire parity。
- 最新 staging 发布证据（2026-09-01）：Auth `24b60577-21ec-4597-9c82-c1d348984d62`、Rate Limit `24cc5e83-77f1-4cef-b6c1-20648b6ecae4`、API Core `c2c85827-5045-4f2b-8a88-1a342017ca02`、API AI `210c6d04-24b5-4b52-8cfb-1cd7f3045d10`、Realtime `adad4ada-e8e8-47da-bc5c-f407f04b7488`、Jobs `4a31f445-8a35-4172-8e1f-52f7a62cae60`、Edge `6ba20ac2-4874-4bc2-acb2-d413375e1c57` 与 Web `d0e7aeb3-e62f-4d3b-8c01-1032261bb75d` 均已发布；新 disposable Better Auth 账号的 authenticated smoke 全部通过（含 Web→Edge→Workers AI chat），health 为 `200`。首次使用已删除旧账号的 smoke token 得到 `409` 后自动回滚，换新账号重发成功。
- External MCP app OAuth namespaced staging seam（2026-09-01）：App D1 已应用 `0115_mcp_app_oauth_generation.sql`，namespaced `/v2/cf/apps/mcp/*` 仍默认 gate=false；随后 exact `/v1/apps/mcp` registration、callback、`/{app_id}/mcp/refresh` 已由 Jobs staging owner 承载。最新发布版本为 Jobs `ac14b2c1-b03c-4f46-8cee-66f315243916`、Edge `9ab249de-8153-45bd-99ad-be1ec6ccdf2c`；无效注册体返回 `422 invalid_app_name`、无效 callback state 返回 HTML `400`、不存在 app refresh 返回 `404 app_not_found`。该 exact seam 已闭合 RFC 7591/PKCE、加密凭据 generation fence、discovery、owner/revision CAS 和 deletion fence；真实 provider replay、Firebase identity/catalog backfill 和 production parity 仍未完成。
- MCP tools projection staging evidence（2026-09-01）：API Core `0bba6506-c0b9-4fac-a3d3-e9ac66ca34f8`、Edge `d07ad9a5-f17b-42ab-b677-a72b17c7239f` 已发布新增 `GET /v2/cf/apps/mcp/tools`。该入口通过 Better Auth owner scope 读取已安装且 `discovery=ready` 的 D1 projection，重新校验工具 schema/大小/重复名称，不返回 provider endpoint、OAuth metadata 或凭据；线上未认证请求为 `401`，`/health` 为 `200`。本地 `test_mcp_app_projection_routes.py` 3 tests、Cloudflare 全量 68/529、typecheck 和 manifest（594 routes）均通过。它只是后续 runtime 的只读输入，尚未执行上游 MCP tool 或回写结果。
- MCP tools/call staging evidence（2026-09-01）：Jobs `a2d7b8b6-4ec8-4196-92fe-9c756cd21522`、Edge `5274ad7b-ba0e-403d-8dfa-879b96aa11de` 已发布 `POST /v2/cf/apps/mcp/tools/{appId}/call`。入口仅接受 Better Auth owner 已安装、authorized、ready 的 app，先校验参数与 deletion fence，再在 Jobs 解密凭据执行 bounded MCP `initialize`/`notifications/initialized`/`tools/call`，支持旧 SSE fallback，provider 401 通过 revision CAS 转为 `reauthorize`；线上未认证返回 `401`、`/health` 返回 `200`。MCP staging suite 16 tests、Edge focused 141 tests、API-AI 102 tests、typecheck 和 manifest（595 routes）通过；结果目前不持久化，未接入旧 Chat runtime。
- Chat compatibility preflight staging evidence（2026-09-01）：`/v2/cf/chat/completions` 对显式 `app_id` + `tools` 先读取同一 MCP projection，projection 缺失返回 `404`，损坏返回 `503`，存在但尚未执行时返回 `409 mcp_tool_execution_not_migrated`，均发生在 quota/provider/message mutation 之前；随后 `/v1/chat/materialize-prompts`、`/v2/chat/materialize-prompts` 和 `/v2/chat/completions` 已切到 bounded Jobs/API-AI staging owner，没有改变旧客户端 production parity 门槛。
- Chat preflight staging deployment（2026-09-01）：API-AI `5e7ce3d8-ecb4-4e2f-b488-cb57d13462a2` 已发布，Edge `/ready` 返回 200 且 `api-ai=200`；未认证 `/v2/cf/chat/completions` 保持 `401`。该部署只更新显式 Cloudflare `/v2/cf/chat/completions` contract，不打开旧 Chat compatibility owner。
- Gemini budget parity staging deployment（2026-09-01）：API-AI `0cceef7d-f7e1-4575-9847-11be239125b3` 已发布；server-paid Gemini adapter 对 generate/stream 请求保持单候选并将 `maxOutputTokens` 限制为 2048，BYOK 上限为 8192，多候选在 provider dispatch 前拒绝。Edge exact `/v1/proxy/gemini*` 仍因 `GEMINI_PROXY_CLOUDFLARE_ENABLED=false` 返回 `503 gemini_proxy_unavailable`；该切片没有改变 legacy owner。
- Gemini Vertex service-account seam（2026-09-01）：API-AI 已加入 `GEMINI_PROXY_PROVIDER=vertex_ai` 的 Workers Web Crypto JWT→Google OAuth token→Vertex REST/SSE/embedding adapter 与 `0117_gemini_vertex_provider.sql` usage provider 扩展；默认仍为 `ai_studio`，未配置 service-account 时 fail-closed。最新 staging 已开启 Gemini exact owner；无 provider secret 的 authenticated JSON/stream probe 返回 `503 gemini_provider_unavailable`，不调用 legacy。ADC/PT、Firebase continuity、旧 Redis quota/cost parity 与生产切换仍未闭合。
- Audio merge staging boundary 已扩展为 legacy MP3 adapter：Jobs 使用 `0105_audio_merge_legacy_jobs.sql`、R2 chunk inventory 和 Worker-safe MP3 encoder，兼容 schema-v1 单文件与 schema-v2 conversation payload，并由 D1 lease/Queue/idempotency 管理；原 `/v2/audio-merge-jobs/run` 现切到 staging Jobs owner。历史 Cloud Tasks/OIDC dispatcher 的调用方仍需迁移到 Better Auth/signed Edge boundary，生产 GCS 回放与生产切换尚未完成。
- 本轮 staging 发布证据（2026-08-31）：远端已应用 `0104_memory_lifecycle_projection.sql`、`0105_audio_merge_legacy_jobs.sql`、`0106_chat_file_thumbnails.sql`；Jobs 版本 `41ef310e-3249-4cf7-b35a-43e9755fd132`、Edge 版本 `793fe363-493b-4809-8f5e-ebfa177303f3`。无凭据请求分别验证 audio merge `401`、lifecycle admin boundary `403`、关闭状态的 `/v1/files` 与 `/v2/files` aliases `404 route not migrated`；已登录浏览器的 `/conversations` 页面正常，浏览器错误/警告为空。由于当前 shell 未提供 staging admin key、Better Auth fixture 和 provider secret，尚未做 lifecycle/audio 的 authenticated positive Queue drain，也未开启原始 chat-file aliases。
- Hume callback staging ingress 已发布在同一 Jobs/Edge 版本；未配置 signing key 时返回 `503 hume_callback_unavailable`，避免接收无法验证的 provider payload。当前 consumer 已落地 receipt-only 处理：对 legacy `results.predictions → models.prosody.grouped_predictions` 做 bounded normalization，把 `COMPLETED`/`FAILED` 结果幂等写入 D1 `cf_hume_webhook_results`，并默认标记 `mapping_status=unmapped`；`0135_hume_task_projection.sql` 与默认关闭的受保护 Jobs apply seam 现在允许 operator 提交 content-bound、destination-bound 的 reviewed `request_id → uid/conversation` projection。D1 deletion/generation/cutover/conversation fences 与幂等 Queue mapping 会阻止猜测身份或跨代回写；该 seam 不写伪造 task/emotion/conversation/notification，不改 exact provider cutover，也不宣称 legacy 业务 parity。真实 source export attestation 与 provider positive probe 仍未完成。
- 上一批 staging 边界发布（2026-08-31）：API Core `1a865464-2c3c-447a-bad5-f62ec231cbd1`、Edge `a9046a33-4b91-489a-8ec8-263134bc639a`、Jobs `dd604220-0f34-40e2-b2d0-096932dcc3b3`；远端 D1 已应用 `0103_data_protection_migration.sql`。真实 Better Auth 账号验证 health=200、What Matters Now/debug=404（认证后无 projection）、migration single/batch/finalize=503（缺少 capability，未写 receipt）、Phone/Twilio=503、legacy Auth/OAuth=503；`POST /v1/personas` 图片创建返回 200，完全重复返回同一 `app_id`，随后删号返回 200。
- Gemini/chat compatibility staging owner（2026-09-01）：API-AI `7f13e50b-e4bb-45f5-909d-a0d39ed73922`、Jobs `ac14b2c1-b03c-4f46-8cee-66f315243916`、Edge `9ab249de-8153-45bd-99ad-be1ec6ccdf2c` 已发布。Gemini exact JSON/stream 在未配置 provider secret 时返回 `503 gemini_provider_unavailable`；Chat exact aliases 由 Jobs/API-AI 处理 bounded 请求，不回落 legacy。两组已计入 staging owner，生产 provider、历史 continuity 和完整 wire parity 仍未闭合。
- Task intelligence staging owner（2026-08-31）：13 条 staged-task/task-intelligence 路径已由 Edge → API Core 承载，`0109_task_intelligence.sql` 与 `0110_task_intelligence_queue.sql` 已应用到 staging；API Core `05a388f9-ba93-4c38-afb4-3efbbcea2114`、Jobs `309ba7d4-0b3f-4998-a25f-40492b0b97e7`、Edge `4ea2dcf7-1a40-4a1f-96aa-c3e4df9f49b8` 已发布。未认证请求对 staged tasks、context snapshot 和 evaluate 均返回 `401`，旧 backend 不会被调用；真实 Better Auth 账号完成 cutover 后创建 staged task 并经 Workers AI evaluate 返回 `200`，删号后 task/intelligence residual 为 `0`。provider 暂时失败会进入 Jobs Queue retry/lease，第三次失败 terminal；该 owner 仅对 completed destination-bound Better Auth 账号开放，旧 Firestore staged-task 历史回放、旧客户端 continuity 和生产切换仍待完成，详见 [task-intelligence-staging.md](./task-intelligence-staging.md)。
- Persona PATCH staging owner（2026-08-31）：`PATCH /v1/personas/{persona_id}` 已由 Edge 以 Better Auth 身份转发到 Jobs，Jobs 只更新 owner-scoped D1 Persona projection，使用 CAS、R2 logo replacement/cleanup 和 account-deletion fence；本地 Persona mutation 4 tests 与 Edge forwarding test 通过。真实 Better Auth 账号创建默认 Persona 后，带 1x1 PNG 的 PATCH 经 staging 返回 `200` 并更新 username，删号后 Persona 行为 `0`（删除 intent 随 Queue 收敛）；历史 Firestore Persona、完整 memory-condensed prompt 和 legacy GCS logo mapping 仍未完成。
- Persona/apps boundary（2026-09-01）：MCP app registration/callback/refresh、Twitter ownership exact route 与 app-owner exact adapter 已切到 Jobs staging owner；Twitter exact owner 由 Edge/Jobs `TWITTER_OWNERSHIP_EXACT_STAGING_ENABLED` 和 RapidAPI secret 双重保护，app-owner exact adapter 另由 `APP_OWNER_MIGRATION_EXACT_STAGING_ENABLED`、Auth anonymous bridge 与 D1 attestation/generation/deletion fences 保护，默认关闭。已迁移的 `POST/PATCH /v1/personas` 正向 owner 不受该开关影响。
- 最新 staging 发布证据（2026-09-01，0125/Provider 更新）：远端已应用 `0125_audio_chunk_import_ledger.sql`；Auth `7e2f3ea4-2a41-4222-adc8-53651332eabf`、API Core `7ea8ae1b-c17e-4b37-8828-9b0156768212`、API AI `0b2adb02-8a36-49ce-8f66-4bdbbf82ddbd`、Realtime `279d5972-4a7e-43f1-800e-5141ab8b08dc`、Jobs `d8e68144-1fef-4464-9b57-e11ee6060056`、Edge `870d1e2f-9302-4c2c-9eef-133c989e785c` 与 Web `06653d71-77f2-4afa-a2a6-878f1dd4249c` 已发布；Cloudflare 78 个测试文件/609 个测试、API Core 377、API AI 114、Web 9/41 均通过，authenticated smoke 与 Edge/Web health=200。Firebase token exchange 已增加响应体/凭据有界校验；`audio:reconcile` 与 Wrapped history planner 均保持 dry-run，不复制或写入生产数据。真实 provider、GCS/Firestore 回放、ffmpeg/pydub parity、memory re-encryption 和 production cutover 仍未完成。
- 最新 staging 发布证据（2026-09-01，本轮）：远端已应用 `0124_app_owner_projection_executor.sql`；Auth `e2ef9a5e-f66d-4427-ba3b-f5a568e97d0f`、API Core `57fb38c3-2195-4065-be4d-ccf70141d174`、API AI `0226e4a7-70ce-4565-a4b9-94d53fb70189`、Realtime `0d1ffe35-f8a5-4b68-870a-b11be239ad2a`、Jobs `6224b42b-0212-4796-bca6-fd540b4eb9a5`、Edge `c0cb6db3-45ee-4f36-9d33-7133927f972e` 与 Web `69650b7d-d155-4fdb-94ca-11f15d735ffa` 已发布；Cloudflare 76 个测试文件/599 个测试、API Core 377、API AI 114、Web 9/41 均通过，authenticated smoke 与 Edge/Web health=200。默认 gate 关闭时 Twitter exact 返回 `503 persona_apps_unavailable`、namespaced 返回 `404 twitter_ownership_unavailable`，owner migration/identity projection 分别返回 `503 app_owner_migration_unavailable`/`503 firebase_identity_projection_unavailable`；未调用 legacy。真实 RapidAPI/Firebase/provider replay、Firestore 历史回放、memory re-encryption 和 production cutover 仍未完成。
- 最新 staging 发布证据（2026-09-01，0126 历史 Phone ledger）：远端已应用 `0126_phone_number_import_ledger.sql`；Auth `811820ef-b780-4e4c-9d21-834e0412d0c2`、Rate Limit `b66cfd8d-f659-4cb7-8f88-04ffdd0fdc2b`、API Core `f2a24eb8-4384-488b-a463-8af9a2598916`、API AI `3a2b7f16-7d52-40e6-a1fd-fbe1339376cf`、Realtime `13add733-023d-4608-b6e0-8b79f754742a`、Jobs `157a48dc-92e0-47f3-93fd-7b3bd261842f`、Edge `6a4337f4-418f-4b1b-8e07-18fbac3a1683` 与 Web `c4454402-edd3-4866-8220-2df49956ec7c` 已发布；Cloudflare 80 个测试文件/618 个测试、API Core 377、API AI 114、Web 9/41 均通过，完整 staging smoke 与 Edge/Web health=200。Persona/App 与 Phone 历史 planner 仍是 dry-run，未写入历史数据；provider 正向验证、Firestore/Firebase 连续性、memory re-encryption、旧 dispatcher/ffmpeg parity 和 production cutover 仍未完成。
- Persona/App 与 Phone 历史回放规划（2026-09-01，本轮）：新增两个永久 dry-run planner，分别校验 `plugins_data` 的 opaque identity/public metadata/encrypted envelope/GCS generation，以及 verified Phone/Twilio 的 AES-GCM ciphertext/proof/hash/SID；输出只读校验或 ledger SQL，不导入 Firestore/GCS、不调用 Twilio、不写 `cf_app_catalog`/`cf_phone_numbers`。新增 `0126_phone_number_import_ledger.sql` 并纳入 account-deletion residual/purge；Persona/App 另新增 `0138_persona_app_history_executor.sql` 的 reviewed public-catalog apply seam，但默认 gate 关闭，仅在 source attestation、generation/deletion fence 和 HMAC plan 审核齐备时写入隐藏 catalog row；当前仍需真实人工审阅导出、Firebase/Firestore identity continuity、memory re-encryption、provider 正向验证与 production cutover。
- `GET/POST /v1/agent/*` staging live probe（2026-08-31）：未认证目录与执行请求均返回 `401`；临时 Better Auth 账号经 Edge 获取目录返回 `200` 与 7 个工具定义，执行 `get_memories_tool` 返回 D1 结果，未知/未迁移的 calendar tool 返回 `404`，响应不含内部 `config` 参数；探针账号已提交删号。
- Stateless chat generate-reply staging live probe（2026-08-31）：API AI `0b8636e3-ed1b-4928-8434-ac470110f238`、Edge `af9be7e8-277b-4392-9614-f19f72928ef7` 已发布；Better Auth 隔离账号经 Edge 生成草稿返回 `200`，未认证为 `401`，非法 history 为 `422`，缺失 app 为 `404`；D1 quota event 独立结算，删号 Queue 完成后 chat session/message/quota event/deletion intent 均为 `0`，仅保留 1 条预期 tombstone。
- Conversation finalization 的 `0094` migration、API Core/Jobs/Edge 已于 2026-08-31 发布（版本 `cb1a8271` / `18df02b0` / `99eb710f`）；隔离账号实测 `queued → running → completed`，会话读回 completed，删号后 finalization job 与 conversation 残留均为 0（仅预期 tombstone）。
- Conversation reprocess 的 `0095` migration、共享 D1 job operation、Jobs 参数保留与派生替换已于 2026-08-31 发布（API Core `23cc4631-1942-40eb-b67d-e8c3186d0786` / Jobs `db0f4aa5-cff3-4be8-acd3-6e817f37ab91` / Edge `ebec0033-35af-4c27-8c08-9cee3078fe2c`）；隔离账号实测从会话创建、参数化 reprocess 到 `running → completed`，读回 completed 与新 action item，删号后相关 D1/vector 残留均为 0（仅预期 tombstone）。
- Conversation merge 的 `0096_conversation_merge_jobs.sql` 已于 2026-08-31 应用到 staging（D1 `995078e7-d4f2-4efd-ad81-22f85ccaf1c8`）；API Core `9768ac95-9961-4b1f-ab74-05b788d1993c`、Jobs `6cdc9ccc-ba04-4a8a-8e6e-92d851223f0f`、Edge `839c648a-6c0a-4da5-872b-1ae75bcb1483` 已发布。隔离 Better Auth 账号通过 Edge 创建两个会话（均 200），`POST /v1/conversations/merge` 返回 `200/merging`，随后读回确定性结果会话 `completed` 且 `merge_metadata.source_conversation_ids` 为 2；删号后远端 D1 的 conversation、merge job、action、memory、vector outbox 均为 0，仅保留预期 deletion tombstone。
- Wrangler 本地 D1 曾实际执行截至 `0052` 的 52 个 App migration（123 条 SQL 命令），验证删除 intent/tombstone 均阻止迟到写入，清理过期 tombstone 后恢复写入；本轮新增 `0068` 并在远端单独验证。
- `0068_vector_projection_outbox.sql` 已在远端 staging D1 执行 9 条命令；Auth、Rate-limit、Realtime、Jobs、Edge、api-core、api-ai 与 Web Worker 已发布新版本，完整 authenticated smoke 通过。
- 候选任务生命周期入口已纳入 Edge → API Core：在尚无 D1 候选存储和 generation fence 投影时，Better Auth 请求保持 404（无认证仍为 401），不会回落到旧 Firestore；manifest 由 104 条 legacy-owned 降至 96 条。Google Calendar grant 的通用 `DELETE /v1/integrations/{app_key}`（含 `gmail` 别名）随后纳入 Jobs Worker；未知 provider 保持 fail-closed 404，legacy-owned 降至 95 条。Sentry feedback webhook/poll 进一步接入 API Core/D1 action-items，保留 installation/ignored、签名校验、provider skip 和 issue-id 幂等语义，legacy-owned 降至 93 条。随后，`PUT /v1/integrations/{app_key}` 的 canonical `google_calendar` 变体接入 Jobs Worker，hyphenated key 同样归一化，其他 provider 保持 404，legacy-owned 降至 92 条；历史 MCP 根路径 `GET/POST /authorize` 与 `POST /token` 现在别名到 Better Auth OAuth Provider，legacy-owned 降至 89 条；Calendar canonical/underscore callback 及通用 Calendar capability callback 现在复用 Jobs 的 D1 grant，legacy-owned 降至 88 条；`POST /v1/workflow-migrations/task-goal-links` 现由 API Core 基于 D1 task/goal projection 执行并写入幂等 receipt，legacy-owned 降至 87 条；`POST /v1/user/persona` 现由 API Core 基于 Better Auth profile 与 D1 app projection 幂等创建默认 Persona，legacy-owned 降至 86 条；`GET /memory/search` 随后切换到 API Core/D1，legacy-owned 降至 85 条；桌面完整 immutable manifest 的 GET/POST 随后接入 API Core/D1，legacy-owned 降至 83 条。
- 桌面 Stable 指针的 `POST /v2/desktop/channels/promote` 已接入 API Core/D1：仅接受 macOS Stable、已保留且通过正常资格证据的 immutable manifest，使用 generation/release CAS 防止并发覆盖；首次指针写入采用 `NOT EXISTS` 条件插入，精确重试幂等，指针存在后 macOS feed/download 以该指针为 authority。Beta admission/promotion、breakglass 和生产发布流水线仍保留 legacy owner。
- 一次性 MCP key 写入 memory/action-item/conversation 后，英文和中文 semantic query 均由 BGE-M3/1024 Vectorize 召回并通过 D1 hydrate；conversation UTC 日期包含/排除过滤在 metadata WAL 收敛后分别命中/不命中。会话 Queue 路径 19.9 秒可搜索，删除后源数据、projection state、outbox 和 key 回读均为 0。
- 隔离 smoke 账号的真实公开删号返回 200，随后 live Queue 完成两次零 residual scan；App D1 intent/cutover/conversation/chat/memory 和 Auth user/session/account 均验证为 0，25 小时 tombstone 已落库。
- `npm run deploy:staging`（2026-08-31）已通过 backend-route/manifest preflight、50 个 TypeScript 测试、349+88 个 Python 测试、Web 测试/build、D1 migration 和 authenticated smoke；本次完整发布的 Worker 版本为 Auth `501c851c-b566-4f2e-bb0c-b63ed764f871`、Rate Limit `9c37a485-a7fb-4551-8afa-5ef15d773bed`、API Core `bbad2cb5-a5f9-4475-9678-42f44a73f2f5`、API AI `5fe35a6d-1555-4036-8c93-5d4e4451b6a7`、Realtime `12f65143-a75f-48bb-80e5-acd85474c6af`、Jobs `c0ad6a20-8a83-49d7-8f62-d60b6fc3757b`、Edge `f5d71850-731d-4207-b4bb-85cdee154c90`、Web `c84dbce5-6a56-4395-b9b9-f0458c32c60a`。
- 2026-08-31 05:07 UTC 的 Web-only staging 发布已完成，Web Worker 版本为 `270a2eaf-bdb2-4044-ae72-05976599027d`；`/api/worker-ready` 返回 200 且 Auth/Core/AI/Realtime/Jobs/Rate-limit 依赖均为 200，`/login` 与 `/settings` 返回 200。
- 2026-08-31 02:28 UTC 的 desktop manifest staging 发布已成功应用远端 D1 `0089_desktop_release_manifests.sql`（含 immutable update/delete triggers）；完整发布脚本随后因 API AI 控制面读取 secrets 的上游 503 自动回滚，Edge/Web 健康仍为 200。按回滚策略仅重新发布本次改动的 API Core（`bf43c655-30b8-401c-8f6a-f85ff7822f0a`）和 Edge（`1438a62f-93e1-4a35-bfef-e760cf0898d9`），Edge 健康为 200，manifest GET/POST 的错误管理 key 分别返回 API Core 专属 403；远端表/触发器存在且当前 manifest 行数为 0。验证用 Better Auth 账号已通过公开删号完成 Queue 清理，Auth user/session/account 与 App deletion intent/cutover 残留为 0，仅保留预期 tombstone。
- 本次发布已把 Calendar callback、task-goal link 和默认 Persona 更新带入 staging；authenticated smoke 全部通过（Edge/Web health 均为 200），不再存在之前的 smoke-token gate blocker。
- Calendar 专项 live 复核（2026-08-31）：临时 Better Auth 账号经真实 Edge → Jobs/Core 验证 onboarding `not_started → skipped → not_started`、`google_calendar` 与 `google-calendar` 状态读取均为 200，未连接 events 返回业务态 400，未配置 OAuth client 返回 503；canonical、underscore 与 `gmail` callback 均返回预期 HTML authentication error（200），不会落到页面 404。验证账号删除后 Auth `user/session/account` 与 App `onboarding/integration/app` 均为 0。
- 浏览器 staging：`/recaps`、`/chat`、`/conversations`、`/memories`、`/my-apps`、`/tasks`、`/settings` 均无新的 API error/404。
- MCP OAuth 根路径/后缀 discovery 的 `GET/HEAD` 均为 200；grant list 未认证为 401、隔离 Better Auth 账号为 200；撤销后的 token 在 Auth live-consent 检查处立即失效。CIMD 因 Workers 无法满足 DNS pinning transport contract 而明确不广告。
- MCP OAuth 根路径别名的本地 Edge 验证覆盖 `GET/POST /authorize`、`POST /token`、查询串/body 保留、cookie/Authorization 透传和不可信身份头剥离；下游统一落到 Better Auth `/api/better-auth/oauth2/*`。
- 已验证的直接接口：daily summaries 200、messages GET/DELETE 200、未认证访问 401、未知 summary 404。
- Calendar staging 数据面已实测：Web `/api/proxy` → Edge → Jobs/Core 的 onboarding status/skip/reset、integration GET/PUT/DELETE、meeting D1 projection 均可用；未连接时 events 返回业务态 `400 Google Calendar not connected`，未认证为 `401`，不会落到 Web 页面 `404`。当前 Jobs staging 仅配置了 token 加密密钥，`GOOGLE_CALENDAR_CLIENT_ID`/`GOOGLE_CALENDAR_CLIENT_SECRET` 尚未配置，因此 OAuth URL 按设计返回 `503 Google Calendar is not configured`；Web Settings 现在会将该配置错误转换为明确的环境提示。真实 Google 授权、事件读取和创建需在补齐 staging OAuth client 后复验。隔离 smoke 账号已删除，D1 未残留 integration、OAuth state 或 reconnect-required 记录。
- Calendar staging live recheck（2026-08-31，本次发布 API Core `bf77c198-590a-4203-a4c3-64ba1122303a`、Jobs `855a508c-2f5c-4b4c-8b58-40a7923d5727`、Edge `47079a0a-d239-4890-8349-2c2c1c29d045`、Web `636dd00a-bc2e-4ffe-ae03-a30ddcde0f4f`）：隔离 Better Auth 账号通过真实 Edge 完成 onboarding reset/skip/reset、`google_calendar`/`google-calendar` 状态、会议 metadata create/list/get，均返回预期 200；未连接 events 返回 400，OAuth URL 返回 503。Web Integrations 页面点击 Google Calendar 后显示“not configured”环境提示，浏览器日志确认是 `/api/proxy/v1/integrations/google_calendar/oauth-url` 的 503，未出现页面 404。异步删除完成后，Auth account、Calendar onboarding/meeting/integration/OAuth state 与 deletion intent 均为 0，仅保留 1 条预期 tombstone。
- Public Trends staging 闭环已实测：远端执行 `0082_trends.sql` 后，`GET /v1/trends` 经 Edge → Python API Core → D1 返回 HTTP 200、`cache-control: public, max-age=60` 和空数组；无趋势数据时保持 fail-closed，不回退到 Firestore。
- Chat-first staging 边界已覆盖：`POST /v1/chat-first/blocks/validate` 经 Edge → Python API Core → D1 做 uid、account-generation 和实体能力校验，未认证为 401，合法 task block 返回 retry-stable opaque id，失配/缺失实体 fail-closed。
- Chat-first live 验证：staging 临时 Better Auth 账号创建 task 后，正确 generation 经真实 Edge → API Core 返回 `200 accepted` 和稳定 `cfb_…` ID；generation `0/2` 均返回 `generation_mismatch`，随后公开删号完成产品/Auth 清理，仅保留预期 tombstone。
- Chat-first deferral 已接入同一 staging 边界：D1 migration `0083_chat_first_deferrals.sql` 保存 generation/continuity 绑定的 24 小时 receipt，重复提交返回相同 receipt，删除流程纳入该表的 residual purge。
- Chat-first deferral live 验证：真实 staging 账号创建 task 后，首次提交返回 200 pending receipt，完全重复提交返回相同 `deferral_id`/`due_at`，修改同一 continuity 的问题返回 409；公开删号后 deferral/action-item residual 均为 0，Auth 指定 user/session/account 均为 0，仅保留预期 tombstone。
- Crisp unread staging 边界已接入：Better Auth → API Core → Crisp API；未认证请求为 401，缺少 Crisp 配置时返回兼容的空消息列表，外部 JSON 异常 fail-closed 为 502，且不持久化凭据或消息。
- 通用 integration status staging 边界已接入：`GET /v1/integrations/{app_key}` 只读 Calendar/task D1 投影中的连接位，Gmail 按已授予 scope 判断，未知 key 返回 disconnected，不暴露 provider 凭据。
- 通用 integration status live 验证：`todoist`、未知 `whoop`、`google_calendar`、`gmail` 均返回 200 且只含连接布尔值，未认证请求为 401；验证账号删除后 App-D1 intent 清零、保留预期 tombstone，Auth user/session/account 均为 0。
- Google-derived integration OAuth URL staging 边界已接入 Jobs Worker：`gmail` 等别名复用 Calendar 的 D1 哈希 state，未知 provider 保留 400，未认证请求保留 401；真实 Google 授权仍受 Worker client secret 配置门槛约束。
- Calendar OAuth 配置优先使用专用 client，缺失时回退到 Better Auth 共用的 `GOOGLE_CLIENT_ID/SECRET`；两者都要求登记 staging callback URI。Jobs 同时接受 canonical `google-calendar` 和旧客户端使用的 `google_calendar` 回调路径，二者共享同一 D1 single-use state/token authority。
- Calendar disconnect 增加 per-user OAuth generation fence：断开时原子删除 integration/pending state 并递增 generation，已消费 state 的 in-flight callback 在写入 provider grant 前执行 D1 CAS；因此 provider exchange 完成于断开之后也不会重新建立连接。该并发回归仅由 mock provider 覆盖，未声称真实 Google provider replay。
- Apple Health staging 数据面已接入：`PUT /v1/integrations/apple-health/sync`、`GET /v1/integrations/apple_health`、连接保存和断开均由 Edge → API Core → D1 `cf_apple_health` 处理；设备摘要有请求/存储大小上限，账号删除 residual purge 与 mutation fence 同步覆盖。
- 数据保护迁移 staging 边界已接入 API Core：`GET /v1/users/migration/requests?target_level=enhanced` 经 Edge → API Core → D1 返回 uid 隔离的 conversation/memory/chat 待迁移项，三个写入/批量/finalize 端点也已由 Edge → API Core 接管并在缺少 completed cutover 或加密 executor 时 fail-closed 503；Jobs 现有默认关闭的 preparation executor 会按 Python 兼容的 HKDF/AES-GCM、conversation zlib→hex marker 和 Queue lease/retry 生成 content-bound artifact，但不写入 canonical source/FTS/reader，也不生成伪造 receipt。真正的 reader/derived-index 闭合、生产回放和 cutover 仍未完成。
- 数据保护迁移清单 live staging 验证：隔离 Better Auth 账号的合法查询返回 200/空清单，未认证为 401，非法 target 为 400；随后通过公开删号完成 Auth user/session/account 清理，App D1 intent 为 0（仅保留预期 tombstone）。
- 桌面更新公共读取切片已接入 API Core/D1：`/appcast.xml` 返回稳定的 Sparkle XML（按 channel 去重并转义 changelog），`/updates/latest` 和 `/download` 只选择最高 live stable release，`/v2/desktop/update-policy` 保留平台、最大 build 和缺省关闭语义。D1 投影尚未回填发布制品，因此 staging 空表按设计返回空 appcast、latest/download 为 404；发布/晋级写入仍由后续发布流水线迁移负责。
- 桌面更新的 v2 公共读取也已接入同一 D1 投影：`/v2/desktop/appcast.xml` 严格区分 stable/beta identity，`/v2/desktop/download/latest`、`/beta`、`/windows` 返回受转义保护的下载落地页，并保持 channel 空槽的 404/fallback 约定；发布元数据写入和晋级仍由后续发布流水线迁移负责。
- Windows electron-updater feed 读取已接入同一 D1 投影：`/v2/desktop/update-feed/windows` 只接受显式 `windows_feed_url`，stable 不回落 beta，beta 空槽可回落 stable；发布回填仍需写入该平台 feed URL。
- API Core 根路径 `/` 已由 Edge 公共代理接管，返回版本化的 Worker 健康 payload；未知路径仍按 legacy 回滚策略处理。
- 桌面预览的当前指针和 immutable manifest 读取已接入 API Core/D1，严格校验 slug、source SHA、派生 app identity、签名 DMG URL 和 HTML 转义；发布接口也已接入同一 D1 projection，release pipeline 仍负责制品构建和回填。
- 桌面预览的 `DELETE /v2/desktop/previews/{slug}` 已接入 API Core/D1，使用独立 `DESKTOP_PREVIEW_PUBLISH_KEY` 和 generation compare-and-delete，仅删除可变指针并保留 immutable manifest；发布流水线仍负责写入 manifest/指针。
- 桌面完整 immutable manifest 的 `POST/GET /v2/desktop/releases*` 已接入 API Core/D1：D1 只追加 manifest JSON 与 detached SHA-256，注册幂等、冲突拒绝，更新/删除由数据库触发器拒绝；API Core 按 v1 contract 校验全部字段，Edge 保留 `secret-key` + `ADMIN_KEY` 管理边界。Stable channel promotion 也已接入同一 D1 pointer/CAS，并在有 pointer 时驱动 macOS feed/download；Beta admission/promotion、breakglass、发布流水线和历史 Firestore 回放仍需下一步接入，当前无投影数据时读取保持 404/503。
- 会话 transcript 的单段、speaker 批量和 bulk segment assignment 均已接入 API Core/D1；bulk 路由使用 updated-at CAS，支持精确 segment id 与已完成会话的 `#index:N` 兼容目标，未解析目标会在写入前失败。
- 当前用户 Persona 读取已接入 API Core/D1：`GET /v1/personas` 仅查询 `owner_uid` 相同且包含 `persona` capability 的投影，未认证为 401、无记录为 404、禁用或损坏投影不泄露；Twitter profile 读取已接入 Jobs Worker/RapidAPI + D1 persona 关联。`POST /v1/user/persona` 继续在 D1 中幂等创建默认 Persona；带图片的 `POST /v1/personas` 与 owner-scoped `PATCH /v1/personas/{id}` 现由 Jobs Worker 使用 R2、D1 与 Workers AI，具备 CAS、logo cleanup 和 deletion fence。历史 Firestore/prompt/cache parity 与 Twitter 验证仍保留 legacy owner。
- Persona 管理员按 username 查询及按 id 删除已接入 API Core/D1：`GET/DELETE /v1/personas/{persona_id}` 复用 `secret-key` + `ADMIN_KEY` 管理边界，仅处理含 `persona` capability 的投影；创建/更新、Twitter 验证仍保留 legacy owner。
- 应用缩略图上传已接入 Jobs/R2：`POST /v1/app/thumbnails` 保持 legacy 的 `{thumbnail_url, thumbnail_id}` 响应，`GET /v1/app/thumbnails/{thumbnail_id}.jpg` 以 immutable 缓存策略流式返回对象；解析失败、超限和缺失对象均 fail-closed。
- Memory import evidence 已接入 API Core/D1：`POST /v3/memory-imports/batch` 按 uid 保存受限来源证据，复用 legacy 的稳定 run/artifact 去重身份，默认只保留标题/摘要，账号删除与用户导出均覆盖新表；提取、接受和晋级仍是独立后续工作。
- Candidate workflow control live staging 验证：隔离 Better Auth 账号通过 Edge → API Core 返回关闭态（200，`workflow_mode=off`、generation `0`、`chat_first_ui=false`），未认证请求为 401；该账号随后完成公开删号及 Auth/App D1 残留核对。
- `/memory/search` 已接入 API Core/D1：默认产品搜索只读取当前 uid 的 active、非 Archive、非 locked、非 user-review=false 记忆，保留分页、查询 token 匹配和产品响应 envelope。`GET /memory/archive/search` 现有第一阶段 staging 边界：`0097_memory_archive_authority.sql` 提供 operator global gate、uid-scoped capability projection、canonical archive projection 和删号 fence；Edge/API Core 只接受显式 `include_archive=true` 且同时具备 D1 gate/control，缺失或 malformed projection、跨 uid、非 active/processed、锁定、tombstoned、restricted 行均 fail-closed。staging 已完成真实正向/负向 probe 和删号残留核对；当前仍没有 control writer、legacy Firestore backfill 或 production owner/cutover 证据，`/memory/vector/search` 继续由 API Core/D1/Vectorize 承载。`GET /memory/admin/users/{uid}/non-active-route-report` 现由 API Core 读取 `0100_memory_admin_report.sql` 的 generation-bound D1 projection，必须具备 `ADMIN_KEY`、完成且 destination-bound 的 cutover 和无 deletion fence；缺失 projection、跨 uid/generation、非法 route 或超过 report 上限均 fail-closed。对应的 `POST /memory/admin/users/{uid}/short-term-lifecycle/run` 已由 Jobs staging owner 承载：`0104_memory_lifecycle_projection.sql` 补齐 D1 lifecycle fields，Jobs 按 legacy effective-expiry/source-tombstone policy 写 generation-bound transition audit，并提供同步 legacy response、Queue lease/retry/terminal recovery 与 idempotency；历史 Firestore backfill 和 production cutover 仍未完成，详见 `short-term-lifecycle-staging.md`。
- Memory search live staging 复核（2026-08-31）：临时 Better Auth 账号经真实 Edge → API Core，在 `/v3/memories` 创建一条 manual memory 后，`GET /memory/search?query=coffee` 返回 200 且命中该 D1 记录；随后 memory 删除和公开删号均返回 200，删除队列完成后 Auth user/session/account、App memory/deletion intent 均为 0，仅保留预期 tombstone。
- `POST /v2/messages` 经最新 API AI staging 实测返回 `200 text/event-stream` 并写入 D1；若 AI 或 D1 binding 缺失，仍按约定返回明确的 `503`，不会回落到旧 chat pipeline。
- Sync v1 staging live recheck（2026-08-31）：Jobs `2a978062-cab4-48e3-88db-4da1a96ab600` 与 Edge `97acf6aa-ef87-4bdb-87d6-ed2107f53e6d` 发布后，未认证 `POST /v1/sync-local-files` 返回 `401`；无 capture provenance 的 multipart 上传安全返回 `503 backfill_capacity`、`Retry-After: 30`，并保留 `Deprecation: true` 与 v2 successor Link。探测账号随后通过公开删号清理。
- Voice chat staging live evidence（2026-08-31）：API AI `efa4c5c7-85a0-4668-afa0-1c446014725c`、Edge `48fa6e53-fe68-4636-b87f-136784c34f83` 发布修复后，隔离 Better Auth 账号上传已知有声 WAV 返回 `200 text/event-stream`（987 字节 SSE），静音 WAV 按约定为空 SSE（0 字节），未认证仍为 `401`。multipart 解析、音频大小/容器校验、D1 speech metering 和 transcript→chat SSE delegation 均有 Python/Edge 回归覆盖，旧 legacy owner 保留为显式 rollback；探针账号已提交删号。
- Calendar staging final probe（2026-08-31）：最新 Edge 健康 `200`；未认证 Google events 为 `401`，隔离账号 onboarding status 为 `200`，未连接 events 为业务态 `400`，OAuth URL 因 staging 未配置 client 为 `503`；不会出现页面路由 `404`，探针账号已提交删号。
- Account-deletion run staging live recheck（2026-08-31）：Jobs `a919224e-2a57-4b9a-b491-6b3f53aab206` 与 Edge `0628bff6-3b50-47a9-9660-a58755d546b7` 发布后，未认证 run 返回 `401`，认证未知 job 返回 `200 dropped/unknown_job`；显式 staging cutover fixture 的公开删号返回 `200`，run boundary 返回 `200 queued`，Queue 完成后 Auth user/session/account、App intent/cutover/conversation/chat residual 均为 `0`，仅保留预期 tombstone。非 Cloudflare-owned 账号仍按设计返回 `503 account_deletion_unavailable`，旧 Cloud Tasks/OIDC production dispatcher 尚未切换。
- Conversation finalization run staging boundary（2026-08-31）：Jobs `a7774cec-a383-439d-a63f-29d77a22e747` 与 Edge `42599424-5129-49e1-b096-d3d3f4acac18` 已发布。未认证请求返回 `401`；Better Auth 隔离账号的未知 job 返回 `200 {status:dropped,reason:unknown_job}`；额外 `uid` 字段返回 `400 invalid_request`，证明 run body 仅接受有界 `{job_id}`。探针账号删号后，Auth `user/session/account`、App `intent/conversation/finalization_job` 均验证为 `0`；本次探针覆盖入口边界，未伪造终态 job 或宣称真实 finalization Queue drain。
- Sync job run staging boundary 已完成代码接线：Edge `/v2/sync-jobs/run` 转发 Better Auth signed context，Jobs 从 D1 按当前 uid 读取 job/lane/lease，仅接受有界 `{job_id}` 并复用 `sync_local_files` fresh/backfill Queue；未知/跨账号、终态、活动 lease、Queue 失败分别保持 dropped、acked、409 locked、503 语义。最新 Jobs `c3d506d4-cfb6-4558-9f92-029bcf6a23a1`、Edge `d959168e-ee8a-4841-9dfe-7179dc62bc42` 的真实边界探针已覆盖 `401/200 dropped/200 queued/200 acked`，fixture 清理后 Auth/App 均无残留；带真实录音的 Queue drain、legacy Cloud Tasks/OIDC dispatcher 切换和生产数据回放仍未完成。
- 最新 staging 发布证据（2026-09-01，本轮 0127/0128/0129）：远端 App D1 已应用 `0127_app_owner_data_projection_attestation.sql`、`0128_chat_history_reconciliation.sql`、`0129_phone_number_import_executor.sql`；Auth `dbddcb97-0bd7-4f43-8a02-8ee91b42a704`、Rate Limit `b733f9cb-da29-4337-82f9-f72b79312bb0`、API Core `71ec3477-8155-440b-99b8-b8f4d4fa75f8`、API AI `e569099f-28bb-4825-aee9-c3040f129868`、Realtime `f3845824-e2db-4a45-9e58-070b4934953e`、Jobs `36fdb378-8b14-4021-93e2-a08844160508`、Edge `6e7d090f-5885-460e-9b44-a76ba1723118` 与 Web `7ad38f83-c1c9-4330-8848-4a4fbd58ac31` 已发布；Cloudflare 82 个测试文件/631 个测试、API Core 377、API AI 114、Web 9/41 均通过，完整 authenticated smoke 与 Edge/Web health=200。新增 app owner data attestation、chat session/message reviewed replay、phone history review/apply 均默认关闭或需人工审阅，不代表 Firebase/Firestore 全量回放、Assistants/provider/file continuity、memory re-encryption、旧 dispatcher/wire parity 或 production cutover 已完成。
- 本轮 live boundary 复核：authenticated exact `POST /v1/apps/migrate-owner` 返回 `503 persona_apps_unavailable`/`no-store`，namespaced `POST /v2/cf/apps/migrate-owner/data-projection` 在缺少 admin key 时返回 `503 app_owner_data_attestation_unavailable`；远端新 attestation/chat-history/phone-review/apply 表均为 0 行，证明迁移只发布了 schema 与受保护入口，没有意外导入数据。
- 浏览器回归（2026-09-01，本轮发布后）：已登录 staging `/conversations` 页面显示正常的 Conversations 空态，DOM 中没有错误页或 API error/404；该 tab 的浏览器 error/warn 日志为空，与 authenticated smoke 的 `conversations=200` 一致。
- ASR 正向 staging probe（2026-09-01，本轮发布后）：仓库内 4.9 秒、16 kHz mono WAV fixture 经 authenticated Edge → API AI `/v2/voice-message/transcribe` 返回 HTTP 200，`stt_provider=workers-ai`、`stt_model=@cf/openai/whisper-large-v3-turbo`，得到非空英文 transcript；验证了真实 Workers AI 推理、D1 fair-use 计量和 API-first 路径，不代表其它语言/长音频/streaming WER 与生产成本基线已完成。
- Desktop TTS Workers AI 发布（2026-09-01，本轮发布后）：API-AI `27b1450a-aae6-4aa4-91f5-2f451e76fa0c` 已发布；`/v1/tts/synthesize` 与 `/v1/tts/synthesize-workers-ai` 均直接使用 `@cf/deepgram/aura-1`，历史 voice id 仅在 Worker 内映射到 Aura speaker，不读取 `TTS_API_KEY` 或调用外部 TTS API。API-AI 全量 116 个测试、TTS native 回归和 manifest/typecheck 通过；不同 speaker 的音质与旧 provider parity 不在本期范围。
- Native realtime/model-pick staging 发布（2026-09-01，本轮发布后）：Edge `156980c5-2070-4e58-bc77-52fe3d5f6e76` 的 `/v2/realtime/session` 已改为 Better Auth → Edge 签发短期 Workers AI ticket，显式 OpenAI/Gemini provider 返回 `409 external_realtime_disabled`，请求体有界且不调用 API-AI；原生 Realtime DO 使用 Workers AI，客户端无需任何 OpenAI/Gemini secret。Edge 同时以 `AI_COMPAT_STAGING_FAIL_CLOSED=true` 在读取请求体前拒绝 `/v1/ai/*` 外部兼容面。API-AI `94c82729-cc31-454f-a8de-ea5ce615c5e2` 让 `/v2/realtime/usage` 接受 `workers-ai` 且不计算外部 provider 成本；`/v1/auto/model-pick` 改为确定性 Workers AI 模型选择与 D1 cache，不再请求 Artificial Analysis 或默认 Gemini。Edge 147 测试、API-AI 117 测试、typecheck、manifest/backend-route 校验通过；公开探针确认 Gemini 入口 `503 + no-store`、通用 AI/realtime/model-pick 未认证为 `401`；随后用一次性 staging `AUTH_DEV_ISSUER_SECRET` 签发 Better Auth JWT 完成认证正向探针：model-pick 返回 `200 provider=workers-ai`，realtime 返回 `200 provider=workers-ai/transport=cloudflare-realtime`，显式 OpenAI realtime 返回 `409 external_realtime_disabled`，通用外部 AI 返回 `503 external_ai_disabled`，探针 secret 已立即删除。该 secret 是认证测试桥，不是 AI provider secret。
- Chat history reviewed apply seam（2026-09-01，已发布但默认关闭）：Jobs 新增 `POST /internal/chat-history/apply`，要求 `ADMIN_KEY` 与至少 32 字节 `CHAT_HISTORY_IMPORT_SIGNING_SECRET`，对 reviewed planner 输出做 bounded body/entry、敏感字段、source-row/import/manifest/batch hash、account-generation 和 deletion-fence 校验；远端 App D1 已应用 `0130_chat_history_apply_receipts.sql`，Jobs 版本 `21d0ee9b-ff58-4e3b-b61b-d0ed292b29ba` 已发布，Edge/Web readiness 均为 200。84 个 Worker 测试/642 个测试、typecheck 和 manifest 均通过；该 seam 不读取 Firestore/GCS，尚未做真实 export apply 或 production owner cutover。
- Exact app-owner staging owner（2026-09-01，已发布但默认关闭）：`POST /v1/apps/migrate-owner` 已由 Edge → Jobs 承载，保留 `old_id` 查询参数、`source_token` body 和 legacy HTTP 200 success envelope。Jobs 先通过 Auth anonymous identity bridge 生成 hash-only evidence，再要求 D1 data attestation、target generation 与双方 deletion fence 后才创建 Queue job；token/raw Firebase UID 不落 D1，失败统一 `no-store`。远端 manifest 已校验为 616 CF / 577 backend / 0 legacy-owned；148 个 exact app-owner/Edge focused tests、typecheck 和 manifest 通过。最新 Jobs `b861f18f-7f9b-46e4-82e3-9c624da3ff5e`、Edge `3d98898d-476c-4e1e-b5ef-4be9d764c121` 已发布；线上 Edge `/health` 与 Web `/login` 均为 200，默认 gate 关闭的 exact probe 返回 `503 persona_apps_unavailable` + `no-store`。真实 Firebase/provider replay、Firestore app/persona 回放、memory re-encryption、旧客户端 parity 和 production cutover 仍未完成。
- Chat/Phone 历史 export handoff（2026-09-01）：新增 `chat:export-verify` 与 `phone:export-verify`，均对原始 UTF-8 export bytes 做 SHA-256、复用 canonical planner，并默认 dry-run；显式 `--apply` 时才通过 Jobs 受保护 endpoint，分别使用 Chat HMAC plan signature 或 Phone review/apply、generation/deletion fence 和幂等 marker。工具不读取 Firestore/GCS/provider，不把 admin/signing secret 放进 plan、body 或输出；真实 Firestore export、人工审核和 production cutover 仍待完成。
- Wrapped history reviewed apply seam（2026-09-01，已发布但默认关闭）：Jobs 新增 `POST /internal/wrapped-history/reviews` 与 `/apply`，远端 App D1 已应用 `0131_wrapped_history_executor.sql`，创建 review batch/items/apply marker 三张表；Jobs 最新部署已包含批量 generation/deletion-fence 复核、JSON 深度/节点与字段白名单、并发 apply 幂等保护。远端迁移复核为无待应用项，三张表已存在；85 个 Worker 测试/647 个测试、typecheck 和 manifest 均通过。该 seam 只接受已在外部完成审阅的有界 Firestore result plan，默认 gate=false，不代表已完成 Firestore/GCS export、历史账号连续性或 production cutover。
- Hume/DLQ staging 发布（2026-09-01）：远端 App D1 已应用 `0132_hume_webhook_results.sql` 与 `0133_queue_dlq_replay.sql`；Jobs `e3c7e48f-b785-4b7f-8668-bc0cbd4c464a` 已注册 `omi-cf-jobs-dlq-staging` consumer，Edge `df882287-31c3-4346-b174-6bc9de9e0c22` 已发布。`/ready` 返回 200；未配置 Hume signing key 时 callback 返回 `503 hume_callback_unavailable`，DLQ replay 默认关闭时返回 `503 dlq_replay_unavailable`，exact app-owner gate 仍按设计返回 `503 persona_apps_unavailable`/`no-store`。本地全量为 89 文件/671 测试；真实 Hume provider callback、历史 DLQ backlog 控制面导出和 replay positive probe 仍未执行。正向探针工具已经补齐，但没有 captured staging message、显式 gate 和受控时间窗前，不把工具存在表述为 live positive evidence。
- Desktop release history staging 发布（2026-09-01）：远端 App D1 已应用 `0134_desktop_release_history_executor.sql`；Jobs `9935cafb-fc9c-4b58-b2ce-253b34463d15`、Edge `53c0b688-a2b6-4b94-8370-554e884e1265` 已发布。`/ready` 返回 200；默认 `DESKTOP_RELEASE_HISTORY_IMPORT_STAGING_ENABLED=false` 的 review endpoint 返回 `503 desktop_release_history_import_unavailable`，空 manifest read 保持 admin `403`。本地全量为 91 文件/679 测试，typecheck、backend route、manifest validation 均通过；Python backfill tests 7 passed。真实 Firestore export、review/apply、artifact copy、channel promotion 与 production cutover 仍未执行。
- Windows release history verifier 与 artifact mirror（2026-09-01）：`.github/scripts/windows_release_history.py` 生成的 plan 现在可经默认关闭的 Jobs reviewed executor 写入 D1 review/transfer ledger，并将精确的 `v<semver>-windows` `.exe`、`.blockmap`、`latest.yml` 从 canonical GitHub URL 流式校验后镜像到 `DESKTOP_UPDATES` R2；Queue 具备幂等、lease/retry、signed redirect、完整 SHA-256/R2 metadata 校验和 digest mismatch 清理。它仍不读取 GitHub 历史、不创建 Windows channel pointer、不改变 public download owner；Windows 历史回放、promotion、生产凭据和 production cutover 仍未完成。
- Hume synthetic Queue positive probe（2026-09-01）：使用一次性 staging signing secret 和符合 provider 嵌套结构的合成 `COMPLETED` callback，Edge 返回 `202 accepted`；远端 D1 观察到 `processing_status=completed`、`prediction_count=1`、`mapping_status=unmapped`，随后删除 event/result 并确认残留为 0。临时 secret 已删除；该证据只证明签名→D1 receipt/result→Queue consumer 链路，不代表真实 Hume provider、`request_id → uid/conversation` 映射或 emotion/notification parity。
- Hume task projection staging 发布（2026-09-01）：远端 App D1 已应用 `0135_hume_task_projection.sql`；Jobs `bac1cbaf-ad67-4314-b7d6-5e77ee8bf7a1`、Edge `4ac2f1ae-9b49-4eca-b320-7bfddc92c061` 已发布，默认 `HUME_TASK_PROJECTION_STAGING_ENABLED=false`。受保护的 `/internal/hume-task-projections/apply` 只接受 reviewed、content-bound 的 Firestore task projection；真实 source export、Firebase/provider identity continuity 和 production cutover 仍未执行。

- Memory Archive reviewed projection seam（2026-09-01，默认关闭）：Jobs 提供 POST /internal/memory-archive/reviews 与 POST /internal/memory-archive/reviews/:reviewId/apply，仅接受外部人工审阅的 Firestore memory plan；每行重算 source-row/import/plan hash，签名绑定 manifest，写入 cf_memory_archive_items 与 cf_memory_archive_applies，重复 apply 幂等，并在 review/apply 两阶段复核 global gate、memory control、cutover generation 和 account-deletion fence。该 seam 不读取 Firestore/GCS/provider、不生成 legacy 数据，远端 backfill、真实 export、历史账号连续性和 production cutover 仍未完成。
- Memory Archive executor staging 发布（2026-09-01）：远端 App D1 已应用 `0135_memory_archive_executor.sql` 与 Hume trigger cleanup `0136_hume_task_projection_trigger_cleanup.sql`；Jobs `22c35c52-f42f-4fa8-9b64-e98b4cc09045` 已发布，Edge/Web readiness 依赖检查均为 `200 status=ready`。`MEMORY_ARCHIVE_IMPORT_STAGING_ENABLED=false` 保持默认关闭，因此未执行真实 Firestore export/apply；新增只读 `memory-archive-reconcile.mjs` 生成与 Jobs handler 同算法的 deterministic plan，验证覆盖 93 个 Worker 文件、689 个测试、typecheck 与 manifest，重复 review 并发幂等也有回归覆盖，真实 backfill、历史账号连续性和 production cutover 仍是 blocker。
- Auth smoke fixture recheck（2026-09-01）：`/auth-issue` 只签发用于 JWT 验证的 identity-only token，不创建 Better Auth `user` 行；因此用该 token 访问 `/v1/users/profile` 得到预期 `410 User not found`，不能作为完整 authenticated smoke 的用户 fixture。用真实 `/api/better-auth/sign-up/email` 创建隔离 Better Auth 用户后，同一路径返回 `200`；两种验证均不需要 OpenAI/Gemini provider secret。
- Workers AI secret inventory recheck（2026-09-01）：staging 的 API AI、Edge、Jobs 和 Realtime Worker 均未配置 `OPENAI_API_KEY`、`GEMINI_API_KEY`、Vertex service-account 或通用 `AI_API_KEY`；API AI 只保留内部断言 secret，Edge 只保留内部断言/BYOK 指纹 pepper，Jobs 只保留内部断言、业务集成 token 加密和管理类 secret。`GEMINI_PROXY_ENABLED=false`、`GEMINI_PROXY_CLOUDFLARE_ENABLED=false`、`CHAT_FILES_WORKERS_AI_ENABLED=true`、`CHAT_WORKERS_AI_ATTACHMENTS_ENABLED=true` 保证 canonical 路径只使用 Workers AI；外部 AI 兼容 adapter 的 provider secret/positive probe 不属于本期部署依赖。Calendar 的 Google OAuth client 是单独的业务集成凭据，未配置时 OAuth URL 按设计返回 `503`，不影响其它 Workers AI 路径。
- Calendar configuration UX recheck（2026-09-01）：Web `e44d2f65-25c3-491a-ab83-0caecdcba4aa` 发布后，已登录 Settings → Integrations 在缺少 Google OAuth client 时正常渲染集成卡片；后端 `503 Google Calendar is not configured` 被转换为环境提示，不再写入浏览器 error/warn 日志。真实 OAuth 连接仍只在补齐 Google client 后验证。

## 当前范围内唯一未完成项

如果要启用真实 Google Calendar 连接，只需在 Jobs Worker 配置
`GOOGLE_CALENDAR_CLIENT_ID`、`GOOGLE_CALENDAR_CLIENT_SECRET`，并在 Google
Cloud OAuth client 中登记
`https://omi-cf-edge-staging.summersmile1984.workers.dev/v2/integrations/google-calendar/callback`；
随后运行 `npm run calendar:positive-probe` 验证授权、事件读取和断开清理。
这两个凭据是 Calendar 业务 API 的 OAuth 凭据，不是 OpenAI/Gemini 或任何 AI
provider secret。当前没有这些凭据时，Calendar OAuth URL 按设计返回
`503 Google Calendar is not configured`，不会阻塞其它 Workers AI 路径。

## 后续可选发布资格（不阻塞当前空数据部署）

1. Twilio、任务集成等其它外部业务只有在明确启用时才配置各自凭据并做 smoke；
   Workers AI 是本期默认生成式 AI，不要求 OpenAI/Gemini secret。
2. authenticated staging R2 multipart probe 已完成；presigned direct-to-R2 只有在
   客户端启用后再补，随后再做中断恢复 live 演练。
3. 为仍使用 Redis 的 planned family 选择 KV/DO/Queues owner，并完成并发、TTL、锁故障测试。
4. 补齐高风险异步任务的 staging Queue/DLQ 正向 probe；当前 no-data 部署不需要导出历史 backlog。
5. 建立 ASR 质量/延迟/成本基线，并为剩余 workstream/screenshot Vectorize projection 建立 recall/hydrate 证据。
6. 通过生产 rollout、监控/告警/成本预算和 legacy cleanup 门槛后，才切生产 DNS/identity。

生产 Firestore/GCS 数据回填、Firebase legacy token 和旧客户端/旧 provider wire
兼容不属于本期阻塞条件；未来若重新引入这些需求，再按独立迁移窗口建立回放与
兼容证据。

权威设计约束和完整验收矩阵见 [`dev/cloudflare-adaptation-plan.md`](../../dev/cloudflare-adaptation-plan.md)。
