# Legacy 路由迁移审计

截至 2026-09-01，`backend-routes.json` 中已没有 `legacy-owned` 路由。这个清单不是把路由简单改成 Cloudflare 代理：只有当数据 authority、认证边界、异步重试和外部 provider 语义都能在 Workers 上闭合时，才允许把 owner 改成 `staging-owned`。auth/oauth、chat compatibility 和 Persona/MCP 的逐路由准入条件见 [`remaining-legacy-contract-gates.md`](remaining-legacy-contract-gates.md)；所有 route 已切到 staging owner，但仍有 provider/backfill/cutover 门槛。

## 分组与迁移前置条件

| 路由分组                             |            当前 legacy 条数 | 代表路径                                                                                  | 主要缺口                                                                                                                       | 下一步                                                                                                                                                                                                                                                                             |
| ------------------------------------ | --------------------------: | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Auth / OAuth / social                |                           0 | `/v1/auth/*`、`/v1/oauth/*`                                                                  | staging Auth/Jobs 已承载 exact callback/token/consent；真实 Firebase/provider、旧客户端 wire parity、历史 catalog 和生产身份连续性仍未验证 | 保持 staging owner 的 fail-closed gate；完成 identity import、provider replay、旧客户端 conformance 和生产回放后再做 production cutover                                                                                                                                                              |
| Phone / external integrations        |                           0 | `/v1/phone/*`、电话 webhook                                                               | Twilio provider secret、真实号码验证和生产历史回填仍未验证                                                                       | 已由 Edge→Jobs staging owner 承载；`0108_phone_twilio.sql` 提供加密号码、verification TTL、quota、CallId/CallSid 幂等和 deletion fence。需完成真实 Twilio staging probe、历史 Firestore 回填和 production cutover 后才算全量迁移 |
| Conversation lifecycle               |                           0 | `/v1/conversations/{id}/finalization`、`finalize`、`reprocess`、`merge`、finalization run | legacy lifecycle path and fan-out                                                                                              | finalize/status/reprocess/merge/finalization run now use shared D1 job projections, Jobs leases, and API Core; merge performs bounded R2 artifact copy before atomic source cleanup                                                                                                |
| Import / files / sync / audio        |                           0 | `/v1/files`、`/v2/files`                                                                 | 历史 Firestore/GCS 与 Assistants/session continuity 仍未完成                                                        | `/v1/files`、`/v2/files` 已由 Jobs staging owner 承载 bounded D1/R2/provider handler；`/v2/audio-merge-jobs/run` 已由 Jobs staging owner 承载 MP3 schema-v1/v2 adapter，但历史回放和生产切换仍待完成 |
| Task intelligence / staged tasks     |                           0 | `/v1/staged-tasks*`、`/v1/task-intelligence/*`、`/v1/what-matters-now*`                   | Firestore staged-task 历史回放、旧客户端 continuity、provider 正向账号探针和生产切换仍未完成                              | 13 条 staged-task/task-intelligence 写入已由 Edge → API Core 承载；`0109_task_intelligence.sql` 与 `0110_task_intelligence_queue.sql` 已应用；两者提供 D1 candidate/recommendation identity、generation/device fence、Workers AI receipt、promotion/outcome transaction、Jobs Queue retry 和 deletion purge inventory。真实 Better Auth 账号已完成 task 创建与 evaluate 200 探针，删号后 task/intelligence residual 为 0；仅 completed destination-bound Better Auth 账号可用，历史账号继续 fail-closed |
| Persona / apps                       |                           0 | `POST /v1/apps/migrate-owner`（Twitter ownership exact route 已 staging-owned）                  | Firebase provider identity、Firestore owner/memory re-encryption、公开 cache/memory side effects、Twitter production/history parity                         | MCP app registration/callback/refresh、Twitter ownership metadata projection 与 owner migration exact adapter 已由 Jobs staging owner 承载；`POST /v1/personas` 与 `PATCH /v1/personas/{persona_id}` 也已迁移，但仍需完整 Firebase/Firestore contract                                                                                                                                                                         |
| Memory admin / Archive               |                           0 | `/memory/admin/users/{uid}/short-term-lifecycle/run`                                      | Archive projection writer、历史 backfill 和 production cutover                                                                    | short-term lifecycle 已切到 Jobs staging owner：0104 将 `cf_memories` 的 captured/expiry/status/processing/source/generation 字段补齐并回填，Jobs 按 legacy effective-expiry 与 source-tombstone policy 写 D1 transition audit；同步请求返回 legacy response envelope，Queue 负责 lease/retry/terminal recovery。远端 migration 已应用，未认证 admin boundary 已实测；带 admin key 的正向/retry/delete fixture 仍待补；`/memory/archive/search` 仍只有 staging-only D1 control/projection 边界，无 control writer、legacy backfill 或生产 owner/cutover 证据 |
| Desktop release mutation/manifest    | 0（路由已迁移，回填未完成） | release pipeline 回填和生产 Firestore→D1 回放                                             | 发布流水线凭据、历史 manifest 回放和生产切换审计                                                                               | immutable manifest、macOS Stable/Beta channel pointer、Beta admission/promotion/breakglass、legacy release bridge（`/updates/releases` POST/PATCH）和 cache-invalidation contract 已进入 D1；所有相关 endpoint 在 staging 由 API Core 提供，下一步回填发布流水线并复验 Beta 晋级族 |
| Metrics / wrapped / analytics        |                           0 | `/v1/wrapped/*`、部分分析端点                                                               | Prometheus/历史分析数据不在当前 D1 authority；Wrapped 历史 Firestore 结果回填和真实 provider 仍未验证 | `/metrics` 仍保持 fail-closed；Wrapped 已由 Jobs staging owner 承载 `0107` D1 result/job、bounded 聚合、Workers AI schema、Queue lease/retry 和 notification outbox。需完成历史回填、真实 provider probe 和生产 cutover |
| Hume callback / provider webhooks    |                           0 | `/v1/agents/hume/callback`                                                                | 外部 webhook schema、幂等、长处理和重试                                                                                        | staging 已切到 Edge→Jobs 的 Hume 签名、时间戳、bounded-body 和 D1 receipt/result boundary；结果保持 identity-neutral，生产 owner 仍需完成 Firestore task/emotion/conversation/notification authority 与 provider positive probe 后再切换                                                                                                                                 |
| Data-protection migration            |                           0 | `/v1/users/migration/*`                                                                   | 加密 payload、迁移 lease、批量 executor 和生产回放仍未闭合                                                                        | 三个写入/批量/finalize 入口已接入 Edge/API Core 的 fail-closed boundary；先完成加密 payload、Queue executor 和生产回放，再开放成功写入                                                                                                                                                                  |
| Chat materialization / compatibility |                           0 | `/v1/chat/materialize-prompts`、`/v2/chat/*`                                              | 旧客户端 tools/provider/session/usage parity、历史 Firestore continuity                                                               | Chat compatibility bounded owner 已由 Jobs/API-AI 承载；`backfill-d1.mjs` 与 `chat-history-reconcile.mjs` 提供有界去敏回放输入，Jobs 另有默认关闭、签名保护的 reviewed apply seam（0130 receipt）；仍需真实历史 export/apply、文件/provider continuity 和旧客户端完整 provider/BYOK/tools/stream wire contract                                                                                                                                 |
| Other legacy proxies                 |                           0 | Gemini proxy                                                                              | provider credentials 与外部应用 owner continuity                                                                               | Gemini exact routes 已由 API-AI staging owner 承载；仍需真实 provider、Vertex/Firebase 与成本/quota parity                                                                                                                                                                                                                                    |

## 当前可执行顺序

Archive projection writer note（2026-09-01）：0135 增加默认关闭的 Jobs reviewed review/apply seam。它只接受外部人工审阅、HMAC content-bound 且逐行重算 hash 的 Firestore memory plan，写入 D1 archive items 与 apply receipts，并在 review/apply 两阶段复核 global read gate、memory control、cutover generation 和 account-deletion fence；配套 `memory-archive-reconcile.mjs` 只读校验外部 export 并生成相同 hash 算法的 apply plan，不读取 Firestore/GCS/provider、不伪造历史数据，因此真实 export/backfill、历史账号连续性和 production cutover 仍是 blocker。

Archive executor staging 发布（2026-09-01）：远端 App D1 已应用 `0135_memory_archive_executor.sql` 与 `0136_hume_task_projection_trigger_cleanup.sql`；Jobs `22c35c52-f42f-4fa8-9b64-e98b4cc09045` 已发布，Edge `/ready` 与 Web `/api/worker-ready` 均为 `200 status=ready`。`MEMORY_ARCHIVE_IMPORT_STAGING_ENABLED=false` 保持默认关闭；重复 review 并发幂等已有回归覆盖，真实 Firestore export/apply、历史账号连续性与 production cutover 仍未完成。

1. 让 release pipeline 使用 `.github/scripts/backfill-desktop-release-manifest.py` 先生成并人工审阅 dry-run plan，再通过默认关闭的 `desktop-release-history` reviewed apply executor 回填已迁移的 D1 immutable manifest；Stable/Beta promotion、legacy release bridge 和 `clear-cache` 已由 API Core/D1 承接，完成 Firestore→D1 回放后再注入发布流水线凭据并复验 Beta 晋级。该工具只读 legacy manifest、验证 v1 digest；apply 由 Jobs 记录 content-bound receipt，并调用 API Core 的既有 immutable manifest contract，不改变 channel pointer。manifest apply 后可另行调用 reviewed-only artifact executor，将精确的 macOS 三项制品从 canonical GitHub release URL 流式校验并镜像到 staging `DESKTOP_UPDATES` R2；这只是 GitHub→R2 transfer ledger，不改变 public URL、channel pointer 或 production owner。Windows 另有 `.github/scripts/windows_release_history.py` 的 bounded export verifier，并已补上独立 reviewed-only artifact executor：它将精确的 `.exe`、`.blockmap`、`latest.yml` 经 D1 review/transfer ledger 和流式 SHA-256 校验后镜像到 staging `DESKTOP_UPDATES`，但不创建 Windows channel pointer 或 public owner。真实 Firestore export/replay、发布凭据、生产切换和回滚证据仍未完成。详见 [`desktop-release-history-reconcile.md`](desktop-release-history-reconcile.md) 与 [`windows-release-history-reconcile.md`](windows-release-history-reconcile.md)。
2. 保持 conversation finalization/reprocess/merge/finalization run、sync-local-files、voice-messages、sync-jobs run 与 account-deletion run 的 staging residual 监控；下一组迁移优先处理 release pipeline 回填。
3. 保持 task-intelligence 的 Cloudflare owner 在新账号上运行并补历史 staged-task 回放；evaluate 的 provider 正向探针、旧客户端 wire continuity 与生产切换仍需单独门槛。

Review queue 已完成第一阶段闭环：D1 canonical memory 写入会产生结构化冲突记录；三个 review-queue 端点由 API Core/Edge 承载；每次读取校验来源 `updated_at` revision 与 SHA-256 content hash，来源变化自动 tombstone；accept/reject/correct/timeout 解析具备 D1 原子写入和幂等状态。该阶段的 producer 覆盖 canonical `/v3/memories` 与 `/v3/memories/batch` 写入，手工已确认的 MCP/developer memory 不会重新进入队列。

本轮新增 `GET/POST /v1/agent/*`：API Core 只返回并执行已由 D1/Workers 实现的对话、记忆和任务工具，动态第三方 App 与未迁移的 provider 工具继续 fail-closed，避免把未实现的能力暴露给模型。每一组迁移都必须同时更新 route manifest、Edge owner、回归测试、删除/残留清理和 staging live evidence；不能仅添加同路径 alias 来降低 legacy 计数。

本轮新增 `GET /metrics` 的 Cloudflare staging boundary：Edge 只转发显式 operational Bearer，不转发 cookie 或内部身份 header；API Core 校验 `METRICS_SECRET`，secret 未配置或 Prometheus scrape authority 未建立时统一返回 `503 metrics_unavailable`，错误凭据返回 `401`。Worker 不生成零值或历史 Prometheus 文本，因此该路由已从 legacy owner 移出，但不宣称指标数据迁移完成；后续需要独立的聚合/保留策略和 scrape authority。

Wrapped 的 `GET /v1/wrapped/{year}` 与 `POST /v1/wrapped/{year}/generate` 已由 Jobs staging owner 承载。旧实现从 Firestore `users/{uid}/wrapped/{year}` 读取/写入状态，生成过程聚合 Firestore conversations/action items，调用 `wrapped_analysis` LLM，并通过本机 executor 与通知路径异步完成；Cloudflare 侧现有 `0107` D1 result/job、bounded 聚合、Queue lease/retry、Workers AI structured output 和 notification outbox 只覆盖新 projection。历史 Firestore result/source 回放、真实 provider probe、旧账号 continuity 与 production cutover 仍未闭合；在此之前不得把 staging owner 误记为历史数据 parity。

本轮新增 `POST /v2/chat/generate-reply`：API AI 使用 Workers AI 或已验证的 OpenAI BYOK 生成无状态草稿，历史只作为受限 prompt 输入，不创建聊天 session、不写入消息；D1 仅记录独立的 `v2_chat_generate_reply` 配额事件并在 provider 返回后结算，失败会关闭预留。Edge、manifest 和回归测试已同步更新。

Chat compatibility 的三个 exact 入口（`POST /v1/chat/materialize-prompts`、`POST /v2/chat/materialize-prompts`、`POST /v2/chat/completions`）已由 Edge 转发到 Jobs/API-AI staging owner，未满足 provider/session 条件时返回稳定的 bounded 4xx/5xx，且不会把请求转发到 legacy。历史 materialization 仍依赖 Firestore continuity/proactive intent，desktop completions 还包含 provider、Redis quota、BYOK、工具和流式响应契约；当前 owner 尚未证明旧客户端与历史数据 parity。

本轮新增 `POST /v1/sync-local-files` 的 Cloudflare owner：旧路径现在由 Edge 转发到 Jobs，与 `/v2/sync-local-files` 共享 multipart staging、R2 对象、D1 content ledger、Queue lane、Workers AI ASR 和清理状态机；响应保留 `Deprecation: true` 与 v2 successor Link。v1 只接受可验证的 fresh capture，历史/无 provenance 的上传会安全返回 `503 backfill_capacity`，客户端应迁移到 v2 并按 job status 轮询；不再执行 legacy inline pipeline。

本轮新增 `POST /v2/voice-messages` 的 Cloudflare owner：API AI Worker 使用有界 multipart parser 与 Workers AI Whisper 完成转写，按 D1 fair-use source 去重计量，再把 transcript 交给 D1-backed `/v2/messages` 生成 SSE。空音频保持空 SSE；不支持的容器、超限和 provider/D1 故障均 fail-closed，不回落到本机临时文件或 legacy inline chat。

本轮新增 `POST /v1/conversations/shared/chat` 的 Cloudflare owner：Edge 从 Cloudflare 的 `CF-Connecting-IP` 生成不可逆 subject，仅用于独立的 per-IP/global Durable Object 限流；转发时使用绑定 method/path/audience/TTL 的 signed public assertion，不伪造 Better Auth 用户身份。API Core 只读取 D1 的 shared owner index/conversation projection，并调用 Workers AI；未签名请求、私有/锁定会话、80KiB 超限、D1/AI 故障分别保持 401/404/413/503。API Core 版本 `caa1bcb0-d066-4752-9495-110077a1f424` 与 Edge 版本 `94d581e0-f07c-4ef8-a574-372da0049974` 已发布 staging；未签名请求为 401、缺失会话为 404，隔离 Better Auth 账号创建 public conversation 后真实 Workers AI 返回 200。删号清理后 D1 conversation/share/intent 残留为 0，仅保留预期 tombstone。

## Staging evidence（2026-08-31）

- 最近一次完整 staging 发布（2026-08-31）的 Worker 版本为：Auth `501c851c-b566-4f2e-bb0c-b63ed764f871`、Rate Limit `9c37a485-a7fb-4551-8afa-5ef15d773bed`、API Core `bbad2cb5-a5f9-4475-9678-42f44a73f2f5`、API AI `5fe35a6d-1555-4036-8c93-5d4e4451b6a7`、Realtime `12f65143-a75f-48bb-80e5-acd85474c6af`、Jobs `c0ad6a20-8a83-49d7-8f62-d60b6fc3757b`、Edge `f5d71850-731d-4207-b4bb-85cdee154c90`、Web `c84dbce5-6a56-4395-b9b9-f0458c32c60a`。
- `POST /updates/releases` 和 `PATCH /updates/releases/promote` 通过 Edge 实测返回 `401 Invalid or missing X-Release-Secret header`（不是 404），证明请求已进入 API Core 的新 owner；staging 当前尚未注入 `RELEASE_SECRET`，因此没有执行真实写入探针。
- `/health` 通过 Edge 实测返回 `200`；`POST /v2/desktop/beta/candidates/reserve`、`POST /v2/desktop/beta/promote-candidate`、`PUT /v2/desktop/beta/admission`、`POST /v2/desktop/beta/breakglass` 在缺少凭据时分别返回 `401`、`401`、`403`、`403`（均非 `404`）。staging 尚未注入 `BETA_PROMOTION_TOKEN`/`GITHUB_TOKEN`，因此没有伪造 Beta 正向 promotion 或 GitHub 证据结果。
- `npm run smoke:staging` 通过：Edge/v1 health、Apple association、OpenAI Apps challenge、公告、趋势、app reviews、支付边界均符合预期；authenticated checks 因未提供 smoke bearer token 而跳过。Calendar Google events 未认证探针返回 `401`（不是 `404`）。
- `/memory/vector/search?query=coffee&limit=3` 通过专用合成账号实测：未认证返回 `401`，Better Auth 认证后空 D1/Vectorize 结果返回 `200` 且 `search_status=ok`、`items=[]`；账号随后通过 `DELETE /v1/users/delete-account` 清理。此次发布版本为 API Core `50fb07d7-f464-43aa-a6ea-9560360c4280`、Edge `a991ab93-8013-40b9-8172-4a2049ffe5e9`。
- Memory review queue staging 数据面已实测：远端应用 `0093_memory_review_queue.sql` 后，API Core `e6e64b52-7215-44c7-9c91-0b5a9d91100a`、Edge `a39cd3d1-3524-472e-8935-8eeabc37e0a2`、Jobs `3f9efdd3-8695-4f16-8ca8-fdeb20df7342` 已发布；临时 Better Auth 账号通过 Edge 连续创建结构化冲突、读取到 `pending` queue、`accept` 得到 `accepted` 与确定性 D1 commit，并提交删号清理。未认证的三个 queue 路由均返回 `401` 而非 `404`。
- Conversation finalization 的 admission/status、D1 revision/job schema、Jobs lease/retry/reconcile 和 API Core Workers AI processor 已实现并有 Python/TypeScript 回归测试；已完成 staging migration 与正向队列验证，但不宣称生产等价。
- Conversation finalization staging live evidence（2026-08-31）：远端应用 `0094_conversation_finalization_jobs.sql` 后，API Core `cb1a8271-22f2-4211-9ba4-94d7463c4745`、Jobs `18df02b0-7ad1-4010-a063-46144d584899`、Edge `99eb710f-03c3-41bf-8f29-763630690393` 已发布。隔离 Better Auth 账号经 Edge 完成 `POST /v1/conversations/{id}/finalize`（200），status 实际经历 `queued → running → completed`（attempt 1），会话读回 `status=completed`；随后公开删号完成 Queue 清理，`cf_conversations` 与 `cf_conversation_finalization_jobs` 均为 0，仅保留预期 tombstone。
- Conversation reprocess 已完成 D1/Queue 闭环：共享 `cf_conversation_finalization_jobs` 新增 `operation/language_code/app_id`，Jobs 重试/租约恢复时保留参数；API Core 重跑 Workers AI enrichment、按来源替换旧 action/memory 派生行与向量删除/重建 outbox，并支持同一 processing job 幂等返回。staging 正向证据见下方。
- Conversation reprocess staging live evidence（2026-08-31）：`0095_conversation_reprocess_jobs.sql` 已远端应用；API Core `23cc4631-1942-40eb-b67d-e8c3186d0786`、Jobs `db0f4aa5-cff3-4be8-acd3-6e817f37ab91`、Edge `ebec0033-35af-4c27-8c08-9cee3078fe2c` 已发布。隔离 Better Auth 账号经 Edge 创建会话（200），提交 `POST /v1/conversations/{id}/reprocess?language_code=en&app_id=calendar-app`（200），status 实际经历 `running → completed`（attempt 1）；会话读回 `completed/discarded=false`，action item 派生数据可读，`external_data` 无内部 claim marker。随后 `DELETE /v1/users/delete-account`（200）完成，tombstone 存在且 conversation/action/memory/reprocess job/vector outbox/vector state 残留均为 0。
- Conversation merge 的 `0096_conversation_merge_jobs.sql` 已于 2026-08-31 应用 staging；API Core `9768ac95-9961-4b1f-ab74-05b788d1993c`、Jobs `6cdc9ccc-ba04-4a8a-8e6e-92d851223f0f`、Edge `839c648a-6c0a-4da5-872b-1ae75bcb1483` 已发布。隔离账号实测两个会话创建、merge admission `200/merging`、Queue 完成后结果会话 `completed` 且来源数为 2；随后删号完成，D1 conversation/merge job/action/memory/vector outbox 残留均为 0，仅保留 deletion tombstone。
- Calendar staging live recheck（2026-08-31）：本次 API Core `bf77c198-590a-4203-a4c3-64ba1122303a`、Jobs `855a508c-2f5c-4b4c-8b58-40a7923d5727`、Edge `47079a0a-d239-4890-8349-2c2c1c29d045` 已发布。隔离 Better Auth 账号经 Edge 完成 onboarding reset/skip/reset、Calendar integration 状态（underscore 与 hyphen alias）、meeting metadata create/list/get；未连接 events 为 400，OAuth URL 因 staging 未配置 client 为 503。Web Settings 点击连接后显示明确环境提示，浏览器控制台未出现 404；异步清理完成后 Auth account、Calendar onboarding/meeting/integration/OAuth state 与 deletion intent 均为 0，仅保留 1 条预期 tombstone。
- Sync v1 staging live recheck（2026-08-31）：Jobs `2a978062-cab4-48e3-88db-4da1a96ab600` 与 Edge `97acf6aa-ef87-4bdb-87d6-ed2107f53e6d` 发布后，隔离 Better Auth 账号经 Edge 调用 `POST /v1/sync-local-files`；未认证返回 `401`，无 capture provenance 的 multipart 上传安全返回 `503`、`code=backfill_capacity`、`Retry-After=30`，并保留 `Deprecation: true` 与 `/v2/sync-local-files` successor Link。探测账号随后通过 `DELETE /v1/users/delete-account` 返回 `200` 清理。
- Voice chat staging live evidence（2026-08-31）：API AI `efa4c5c7-85a0-4668-afa0-1c446014725c`、Edge `48fa6e53-fe68-4636-b87f-136784c34f83` 发布修复后，`POST /v2/voice-messages` 已声明 API AI/Workers AI owner；隔离 Better Auth 账号上传已知有声 WAV 返回 `200 text/event-stream`（987 字节 SSE），静音 WAV 按约定为空 SSE（0 字节），未认证返回 `401`。有界 multipart/容器校验、speech fair-use D1 source 去重和 transcript→chat SSE delegation 由 `test_voice_transcription_routes.py` 与 Edge forwarding test 覆盖，未配置或异常 provider 均返回稳定 4xx/5xx，不会启动 legacy pipeline；探针账号已提交删号。
- Calendar staging final probe（2026-08-31）：最新 Edge 健康 `200`；未认证 Google events 为 `401`，隔离账号 onboarding status 为 `200`，未连接 events 为业务态 `400`，OAuth URL 因 staging 未配置 client 为 `503`，页面不会出现路由 `404`；探针账号已提交删号。
- Account-deletion run staging live recheck（2026-08-31）：最新 Jobs `a919224e-2a57-4b9a-b491-6b3f53aab206` 与 Edge `0628bff6-3b50-47a9-9660-a58755d546b7` 已发布。未认证 `POST /v1/users/account-deletion-wipes/run` 返回 `401`；Better Auth 认证的未知 job 返回 `200 {status:dropped,reason:unknown_job}`；为隔离账号建立显式 `isolated-staging-v1` cutover fixture 后，公开删号返回 `200`，run boundary 返回 `200 {status:queued}`，Queue 实际经历 `quiescing → purging`（attempt 1/2）并完成 Auth user/session/account、App intent/cutover/conversation/chat residual 全部为 `0`，仅保留预期 tombstone。非 Cloudflare-owned 新账号公开删号返回 `503 account_deletion_unavailable`，保持 fail-closed；旧 Cloud Tasks/OIDC production dispatcher 尚未切换。
- Limitless import staging live evidence（2026-08-31）：远端应用 `0098_limitless_import_jobs.sql` 后，API Core `2b0392e1-a24f-4a47-96ba-3c07513e7a3b`、Jobs `dd389221-e3b4-4258-bb86-df3dc359c569`（修复 FTS 触发器计数后重新发布）、Edge `04a08097-13c4-4409-9b94-97a841f21434` 已发布。未认证 `POST /v1/import/limitless` 返回 `401`；隔离 Better Auth 账号上传真实 ZIP 返回 `202`，Queue 完成后 job 为 `completed`、`total_files=1`、`processed_files=1`、`conversations_created=1`，D1 读回 Limitless conversation/transcript；相同 ZIP+language 再次上传返回同一 job（`200`），完成态 cancel 返回 `409`，删除 job 返回 `200` 并清除 staged R2 object。随后新增的 `DELETE /v1/import/limitless/conversations` 只删除当前 uid 的 `source='limitless'` conversation，清理分享索引并写入 Vector outbox，删号围栏时返回 `409`；两个探针账号均经公开删号完成，`cf_import_jobs`、`cf_conversations`、deletion intents 均为 `0`，每个账号仅保留预期 tombstone。该 owner 仍是 staging-only；历史 Firestore job backfill、legacy lifecycle notification/AI enrichment 和 production cutover 尚未做。
- Memory admin / metrics staging boundary live evidence（2026-08-31）：API Core `c0be35e9-805c-4d3b-9067-58d5099d17d0`、Edge `a6da3950-ab6e-4675-a243-90432bf20cc6` 已发布；`GET /metrics` 未认证和任意 bearer 均返回 `503 metrics_unavailable`（未配置 `METRICS_SECRET`，不生成伪 Prometheus 文本），`GET /memory/admin/users/{uid}/non-active-route-report` 未提供 admin key 返回 `403`。真实指标聚合、memory admin projection writer 与短期 lifecycle Queue/Firestore backfill 仍未完成。
- Chat files staging boundary live evidence（2026-08-31）：Jobs `3b1df09b-08ff-42aa-acc4-515cce592927` 绑定独立 `omi-cf-chat-files-staging` R2，API Core/Edge 同批发布；Better Auth 认证 GET `/v1/cf/chat-files` 返回 `200 []`，staging 未配置 OpenAI Files key 时 text upload 返回 `503 provider_unavailable`，未写入 provider id；删号后 `cf_chat_files=0`、deletion intent=0、tombstone=1。随后 `/v1/files`、`/v2/files` aliases 复用该 canonical Jobs handler 并切到 staging owner；图片 thumbnail、Assistants session continuity、provider 正向 key 和历史 Firestore/GCS backfill 仍未完成。
- Agent tool directory staging live evidence（2026-08-31）：API Core `70bd49eb-f0db-4338-a166-0612af37eda7`、Edge `79ba9a42-e9fc-4819-b5ac-778487a5ee8b` 已发布；未认证 `GET /v1/agent/tools` 与 `POST /v1/agent/execute-tool` 均返回 `401`，Better Auth 认证后目录返回 `200` 与 7 个 Cloudflare-native 工具定义，执行 `get_memories_tool` 返回 D1 结果，未迁移的 `get_calendar_events_tool` 返回 `404`，响应不含 `config`，临时账号已提交删号。
- Stateless chat generate-reply staging live recheck（2026-08-31）：API AI `0b8636e3-ed1b-4928-8434-ac470110f238`、Edge `af9be7e8-277b-4392-9614-f19f72928ef7` 已发布。隔离 Better Auth 账号经真实 Edge 生成草稿返回 200；未认证、非法 history、缺失 app 分别返回 401/422/404。请求只产生独立 quota event，不写 chat session/message；公开删号完成后 quota/session/message/deletion intent 均清零，仅保留预期 tombstone。
- Public shared chat staging live probe（2026-08-31）：API Core `caa1bcb0-d066-4752-9495-110077a1f424`、Edge `94d581e0-f07c-4ef8-a574-372da0049974` 已发布。未签名请求为 401，缺失会话为 404；隔离 Better Auth 账号写入 public conversation 后真实 Workers AI 返回 200。公开删号完成后 D1 conversation/share/intent 残留为 0，仅保留 1 条预期 tombstone。

## 本轮 authority 核对（2026-08-31）

- Limitless import 第一阶段已完成 staging 闭环：`POST /v1/import/limitless` 在 Jobs Worker 使用有界 multipart parser，将 ZIP staged 到 ASSETS 的 `imports/{uid}/{job_id}.zip`，以 uid+内容指纹在 D1 `cf_import_jobs` 幂等登记并投递 `limitless_import` Queue；consumer 对 ZIP central directory、entry 数量/展开大小/压缩比、traversal、坏 UTF-8、CRC/deflate 做 fail-closed 校验，再将符合 Limitless filename/quote contract 的 transcript 以确定性 conversation id 写入 `cf_conversations`。API Core 承接 list/status/cancel/terminal delete（uid-scoped CAS 与 R2 cleanup），以及按 source marker 删除该 uid 的全部 Limitless conversation（分享索引清理、Vector outbox 删除标记、删号围栏）；account deletion residual 同时覆盖 D1 job 表和 `imports/{uid}/` 前缀。该阶段保留 light-import 语义（不执行 legacy lifecycle notification/AI enrichment）；真实 staging ZIP、幂等、终态删除和删号残留已实测。历史 Firestore job backfill、legacy enrichment 和 production cutover 仍未完成。
- Import/files/sync/audio 当前已无 legacy-owned 路由；Limitless 的 6 条路由、chat files 两个 exact alias 和 audio merge 均已具备 staging Worker contract：

  | 路由                                   | 现有 legacy authority                                                                                                                                                              | 当前 Cloudflare 缺口                                                                                                                                                   | 可切 owner 的最小闭环                                                                                                                                                                                                                                         |
  | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | `GET /v1/import/jobs`                  | Cloudflare API Core 查询 D1 `cf_import_jobs`，按 uid/created_at 有界排序                                                                                                           | 不读取历史 Firestore job；旧 job 需单独回放                                                                                                                            | 已闭合：uid-scoped index、上限 1000、deletion fence；仅返回 Worker authority 创建的 job                                                                                                                                                                       |
  | `GET /v1/import/jobs/{job_id}`         | Cloudflare API Core 查询 D1 `cf_import_jobs` 并校验 uid                                                                                                                            | 历史 Firestore job 不在 D1 projection                                                                                                                                  | 已闭合：uid + job_id 主键、状态/计数/error 字段和 404 跨账号隔离                                                                                                                                                                                              |
  | `POST /v1/import/jobs/{job_id}/cancel` | Cloudflare API Core D1 CAS：仅 pending/processing 可取消                                                                                                                           | Queue 消息不会被撤回，由 consumer 读取 cancelled 状态并清理 staged object                                                                                              | 已闭合：原子状态转换、lease 清除、deletion fence；重复取消返回 409                                                                                                                                                                                            |
  | `DELETE /v1/import/jobs/{job_id}`      | Cloudflare API Core 仅允许终态删除，先删 `imports/{uid}/` R2 object 再删 D1 job                                                                                                    | 不删除已导入的 `cf_conversations`；R2 删除失败保持 job                                                                                                                 | 已闭合：uid 前缀校验、终态幂等边界、account-deletion residual sweep                                                                                                                                                                                           |
  | `POST /v1/import/limitless`            | Cloudflare Jobs Worker 有界 multipart ZIP → ASSETS staged object → D1 `cf_import_jobs` → `limitless_import` Queue；consumer 解析后写 D1 conversation/transcript                    | 暂无历史 Firestore job backfill、legacy lifecycle notification/AI enrichment                                                                                           | 第一阶段已闭合：D1 authority、generation/deletion fence、Queue lease/retry、entries/展开大小/压缩比/traversal/坏 UTF-8/CRC fail-closed；真实 staging ZIP/Queue drain、幂等、终态删除和删号残留已验证；仍缺历史回放、legacy enrichment 与 production cutover                                                               |
  | `POST /v1/files`、`POST /v2/files`     | 两个 endpoint 的历史实现写本机 temp，并依赖 Pillow、OpenAI Files/Assistants/vision、Firestore chat session 与 GCS thumbnail        | Jobs 已提供同一 canonical D1/R2/provider handler；当前 staging alias 已开启并对无效 multipart 返回稳定 400，未配置 provider 时 fail-closed；仍缺 Assistants thread/session compatibility、下游 D1 文件读取和历史 Firestore backfill | staging owner 已切换；先完成 downstream session/provider migration 与历史回放，再做 production cutover，不把 alias 验证误记为历史数据 parity |
  | `POST /v2/audio-merge-jobs/run`        | Cloud Tasks OIDC payload；legacy `sync_executor`/pydub 从 GCS/Firestore conversation 读取 chunks，生成 MP3 并写回 playback/conversation metadata，依靠 legacy distributed run lock | Jobs `0105_audio_merge_legacy_jobs.sql` + R2 source inventory + Worker-safe MP3 encoder 已兼容 schema-v1 单文件和 schema-v2 conversation payload，并提供 D1 lease、Queue retry、fingerprint/idempotency、generation/deletion fence | staging 已切到 Jobs owner；仍需把历史 Cloud Tasks/OIDC dispatcher 改接 Edge/Better Auth signed boundary，并完成生产 GCS 回放与生产切换；不能把 staging 验证误记为生产 parity |

  现有 `/v1/cf/assets` 只能证明通用 R2 bytes 的读写和清理可用，不能单独证明 chat-file metadata/provider 兼容；`legacy_audio_rebuild` 现在由 `/v1/sync/audio/:conversationId/urls` 缓存 miss 与 `/precache` 共用，前者返回 `pending` 并幂等投递 D1 `cf_jobs`，后者仍是显式 readiness/rebuild 边界，而 `/v2/audio-merge-jobs/run` 由独立 MP3 adapter 处理。Limitless、files 与 audio owner 均为 staging-only；历史回放和 production cutover 仍未完成。

- Chat-file staging boundary 的 provider 与专用 `CHAT_FILES` R2 binding 均归 Jobs Worker 管理；API Core 不绑定该 bucket，也不复制一份 metadata writer，避免出现双 authority。非图片 direct OpenAI Files 使用 `purpose=assistants`；配置 `IMAGES` 和缩略图 HMAC secret 后图片使用 `purpose=vision` 并生成私有 128px JPEG，否则返回 `503 thumbnail_unavailable`。`/v1/files`、`/v2/files` 已开启同一 canonical handler 的 staging aliases，并已完成 owner 命中与无效 multipart 边界验证；Assistants session continuity、D1 下游读取和历史 Firestore/GCS backfill 仍未完成。

- Archive 第一阶段边界已落地但不等同于历史数据迁移：`0097_memory_archive_authority.sql` 新增 global read gate、uid-scoped `cf_memory_control` 和 `cf_memory_archive_items` canonical projection，均带 account-deletion mutation fence；API Core 的 `GET /memory/archive/search` 只读取显式 Archive 请求、当前 uid/current generation、active+processed+active-source、未锁定且无 restricted sensitivity 的行。缺失或 malformed control/gate 返回 403，D1 故障返回 503，绝不读取 legacy Firestore 或自动补 capability。Edge forwarding、backend route inventory、routes manifest 和 residual surface 已同步并由定向回归覆盖；API Core `564517a5-3a90-43f9-95e7-a9764c42f368`、Jobs `f0e20f55-9288-40d4-bbea-2323cd405573`、Edge `8beb4275-292c-4d4b-8965-4055fb21a918` 已发布 staging，真实 projection 正向查询返回 200，缺少显式请求返回 403；删号后 archive/control/intent 残留为 0，仅保留 1 条 tombstone。当前仍没有 projection writer、Firestore backfill、operator gate management endpoint 或 production owner/cutover 证据。

- Short-term lifecycle 的 Cloudflare authority 已补齐 `0102_memory_short_term_lifecycle.sql` + `0104_memory_lifecycle_projection.sql` 与 Jobs 路由：D1 control row 从 completed destination-bound cutover generation-bound 初始化，run row 以 `(uid, run_id, request_fingerprint)` 幂等登记；Jobs policy reader 使用 `min(expires_at, captured_at + 48h)`，仅处理 active/processed/short_term/未删除未失效 rows，source tombstoned/purged 写 `source_tombstoned`，其余到期 rows 写 `remain_short_term` + `requires_lifecycle_decision` audit。transition writer 对每个 idempotency key `ON CONFLICT DO NOTHING`，同步 admission 在可行时返回 legacy response envelope，Queue consumer 在 D1 lease 下执行并对 transient D1 error 重新入队，达到上限后 terminal-fail；Edge 与 backend route inventory 已切 staging owner。远端 migration 已应用，未认证 admin boundary 已实测；带 admin key 的正向/retry/delete probe、历史 Firestore backfill 与 production cutover 尚未做。

- Task intelligence / staged-tasks staging owner（2026-08-31）：13 条 staged-task/task-intelligence 路径已由 Edge → API Core 承载。`0109_task_intelligence.sql` 与 `0110_task_intelligence_queue.sql` 已应用到 staging；API Core `05a388f9-ba93-4c38-afb4-3efbbcea2114`、Jobs `309ba7d4-0b3f-4998-a25f-40492b0b97e7`、Edge `4ea2dcf7-1a40-4a1f-96aa-c3e4df9f49b8` 已发布；未认证边界为 `401`，真实 Better Auth 账号经 Edge 创建 staged task 并由 Workers AI evaluate 返回 `200`，删号后 task/intelligence residual 为 `0`。provider 失败会释放 lease、发布 Jobs retry message，第三次失败才 terminal `failed`，且不调用 legacy。该 owner 仅适用于 completed destination-bound Better Auth 账号；Firestore staged-task 历史回放、旧客户端 continuity 和生产切换仍未完成，详见 `task-intelligence-staging.md`。
- Task intelligence/Wrapped recheck（2026-08-31）：13 条 task-intelligence/staged-task 入口已由 Edge → API Core 承载；未认证请求返回 `401`，认证但未完成 destination-bound cutover 的账号保持 `task_intelligence_unavailable`，不调用 legacy。Task intelligence 的 D1/Jobs Queue/Workers AI retry、lease、input snapshot、deletion fence 测试通过；由于 staging 尚未注入 task/Wrapped provider fixture，真实 positive provider drain 与历史 Firestore backfill 仍未验证。Wrapped 两条路径的本地 D1/Queue/Workers AI 聚合、retry、notification idempotency、deletion fence 测试继续通过。
- Persona/apps fail-closed live probe（2026-08-31）：同一 Edge 版本对 PATCH Persona、Twitter ownership、MCP app/callback/refresh 和 owner migration 入口均返回 `503 persona_apps_unavailable` 且带 `cache-control: no-store`，不会读取或转发请求体；随后 MCP app/callback/refresh 与 Twitter ownership 已切到独立 Jobs staging owner，`POST /v1/personas` 的已迁移正向 owner 不受该开关影响。该条是历史 fail-closed 证据，当前 exact Twitter owner 仍由显式 gate/secret 保护。

- Data-protection migration 的三个写入入口（single、batch、finalize）已接入 Edge/API Core 的 staging fail-closed boundary。缺少 completed cutover 或加密 executor 时稳定返回 503，不调用 legacy、不创建伪造 receipt；真正的加密 payload、Queue executor、lease 和生产回放仍未完成。

- Hume callback 已完成 staging ingress boundary：Jobs 仅接受带 `X-Hume-AI-Webhook-Signature` 与 `X-Hume-AI-Webhook-Timestamp` 的 bounded raw body，按官方的 `payload.timestamp` HMAC-SHA256 contract 做时间窗口和 constant-time 校验，在 D1 `cf_hume_webhook_events` 保存 provider job receipt/hash 并幂等 admission 到 Jobs Queue。Queue consumer 现在按 legacy prosody 嵌套结构将 `COMPLETED`/`FAILED` callback 解析为 bounded `cf_hume_webhook_results`，并以 `processing_status=completed` 幂等结算；结果默认保持 `mapping_status=unmapped`，不读取或写入 legacy Firestore task、emotion、conversation、notification。`0135_hume_task_projection.sql` 与受保护的 Jobs apply seam 现在承载 reviewed `request_id → uid/conversation` projection，并由 D1 deletion/generation/cutover/conversation fences 约束；仅在该 projection 存在时才可标记结果 `attested`，不改变 exact provider callback 或宣称 production parity。真实 source export attestation 与 provider positive probe 仍是 cutover blocker。签名格式依据 [Hume webhook 文档](https://dev.hume.ai/docs/speech-to-speech-evi/configuration/webhooks/)。
- Hume live boundary probe（2026-09-01）：D1 的 `0102_hume_webhook_events.sql`、`0132_hume_webhook_results.sql` 已应用，Jobs `f67eedcb-d7de-48a3-9f2d-ffc7b5a4247a` 与 Edge `df882287-31c3-4346-b174-6bc9de9e0c22` 已发布 staging；Hume signing key 未配置时 callback 稳定返回 `503 hume_callback_unavailable`，`/ready` 为 `200`。receipt-only consumer 已能将 bounded normalized result 幂等写入 D1；未执行正向 provider callback，仍不宣称 Firestore task→uid/conversation 或 emotion/notification parity。
- Hume synthetic Queue positive probe（2026-09-01）：临时 staging signing secret 下，符合 provider 嵌套结构的合成 `COMPLETED` callback 经 Edge 返回 `202`，远端 D1 result 进入 `processing_status=completed` 且 `prediction_count=1`；event/result 随后已删除并确认残留为 0。临时 secret 已删除；这只证明签名、receipt、Queue consumer 和清理闭环，不代表真实 Hume provider 或 `request_id → uid/conversation` authority。
- 本轮 audio/lifecycle staging recheck（2026-08-31）：远端 `0104`/`0105`/`0106` migration 已应用，Jobs `41ef310e-3249-4cf7-b35a-43e9755fd132` 与 Edge `793fe363-493b-4809-8f5e-ebfa177303f3` 已发布；无凭据 probe 得到 audio merge `401`、lifecycle admin boundary `403`，关闭的 `/v1/files` 与 `/v2/files` aliases 得到 `404 route not migrated`。浏览器登录态 `/conversations` 正常且无 console error/warn。缺少 staging admin key/fixture/provider secret，因此未宣称 authenticated positive Queue drain 或生产 parity。

- Files 两个 exact alias 已按单一 Worker admission/Queue/metadata boundary 切到 staging；仍需下游 session/provider migration、历史 Firestore/GCS backfill 和 production cutover，不能把 staging owner 误认为历史 parity。
- `POST /v1/personas` 与 `PATCH /v1/personas/{persona_id}` 已切到 Jobs/R2/D1/Workers AI staging owner；PATCH 使用 owner-scoped D1 projection、CAS、版本化 R2 logo replacement 和 account-deletion fence，未回填的历史 Firestore Persona 不会被伪造为可用。MCP registration/callback/refresh、Twitter ownership exact route 与 app-owner exact adapter 也已切到 Jobs staging owner；历史 owner side effects 仍未闭合。
- 为避免 staging 把 Persona/app 凭据、OAuth code、MCP server URL 或 provider state 继续送入 legacy，Twitter exact owner 只有 Edge/Jobs `TWITTER_OWNERSHIP_EXACT_STAGING_ENABLED=true` 且 RapidAPI secret 存在时才会执行；`POST /v1/apps/migrate-owner` 另受 `APP_OWNER_MIGRATION_EXACT_STAGING_ENABLED=false` 与身份/数据 attestation gates 保护。Persona/apps 当前无 legacy-owned 路径；非 staging 环境未设置开关时仍保留 legacy forwarding。
- Persona/apps staging seam 发布（2026-09-01）：远端 App D1 已应用 `0121_twitter_ownership.sql`、`0122_app_owner_migration.sql`；Twitter ownership exact owner 与 app-owner migration D1/Queue admission、lease/retry、幂等和 deletion fence 已部署。Twitter exact owner 默认 gate/secret 关闭，authenticated probe 返回 no-store `503`；它不承接 Firebase provider proof、Firestore Persona/Memory side effects 或旧 wire contract。
- Firebase anonymous identity projection seam（2026-09-01）：Auth 新增内部 `POST /internal/firebase/anonymous-identity`，通过 Identity Toolkit lookup、JWT claim/provider/revocation checks 后只返回 HMAC 派生 source evidence；Jobs 新增 `POST /v2/cf/apps/migrate-owner/identity-projection`，应用 `0123_firebase_anonymous_identity_projection.sql` 后以 target generation、attestation expiry、冲突/重放和双方 deletion fence 写入 D1。Edge 已接入 namespaced path；`FIREBASE_IDENTITY_PROJECTION_STAGING_ENABLED=false` 时保持 `503 firebase_identity_projection_unavailable`。该 seam 不保存 raw token/UID，也不等同于 Firestore app/persona/memory migration 或 exact owner cutover。
- Persona/apps seam Edge recheck（2026-09-01）：Edge 已接入 namespaced Twitter ownership、Twitter exact owner 与 app-owner migration/status；Twitter exact owner 只有 Edge/Jobs `TWITTER_OWNERSHIP_EXACT_STAGING_ENABLED=true` 且 RapidAPI secret 存在时才会执行，默认 gate=false 时返回 no-store `503`，app-owner exact adapter 也由独立 gate、Auth anonymous bridge 和 data attestation 保护。manifest 当前已是 0 条 legacy-owned。此次只验证 Edge→Jobs 边界、D1 profile/timeline projection 和凭据隔离，不等同于 Firebase/provider/history parity。
- X OAuth disconnect fence（2026-09-01）：`cf_x_oauth_fences` 为每个 uid 保存单调 `oauth_generation`；OAuth state 在生成时捕获当前 generation，callback 原子消费 state 后，只有 generation 仍匹配时才允许写入加密 X connection。disconnect 会在同一 D1 batch 删除 pending state、推进 generation 并清除连接凭据，因此 provider token exchange 与 disconnect 并发时不会恢复已撤销连接；残留扫描/purge 也覆盖 fence 表。该测试闭环不代表真实 X provider replay、历史 token 回放或 production cutover 已完成。
- `/v1/oauth/*` 仍不能由 Better Auth session 直接替代：legacy token 路径验证 Firebase ID token、应用启用/付费状态及 OAuth state/PKCE；新增的 `/v2/cf/oauth/*` 已有 Better Auth + App D1 的 hash-only state/CSRF、setup/paid/tester admission、install CAS 和 deletion fence，但尚未承接精确 legacy response、Firebase token continuity 或历史 Firestore catalog。外部 `/v1/apps/mcp` registration/callback/refresh 已切到 Jobs staging authority（加密 credential、discovery、CAS），但 provider secret、Firebase identity 和历史 Firestore catalog 回放仍未完成。Persona PATCH 当前只覆盖 D1-owned rows，legacy memory-condensed prompt、Firestore/GCS historical mapping 和 Redis public-cache invalidation 尚无等价 authority；Twitter ownership 依赖 Firebase provider lookup 与外部 Twitter verification。
- Auth/OAuth exact staging owner live recheck（2026-09-01，历史发布）：Auth `04d87a18-4254-427b-a33b-9dce7d22dd6a`、Jobs `1ad49938-1527-4197-bb5a-936949c89a75`、Edge `88e07384-db7f-4fce-a305-bad9100ec89f` 与 Web `22def64c-3afb-46c1-b8c5-890d623ae2ba` 已发布。Edge → Auth/Jobs 的 exact `/v1/auth/*` 与 `/v1/oauth/*` owner wiring、bounded multipart 表单、不缓存错误和不调用 legacy 的回归测试均通过；无 Google/Apple/Firebase provider secrets 时，真实请求按设计返回 `503`/`cache-control: no-store`，不伪造 token 或 uid。当时 manifest 为 614 CF / 577 backend，剩余 2 条 legacy-owned（Twitter ownership、apps/migrate-owner）；新 disposable Better Auth 账号的全量 authenticated smoke 通过。
- Phone/Twilio staging owner（2026-08-31）：六个 `/v1/phone/*` 入口已由 Edge→Jobs 承载，D1 `0108_phone_twilio.sql` 保存加密 caller-ID、verification TTL、月度 quota、CallId/CallSid 幂等和 deletion fence；Jobs 通过 Twilio REST、Voice JWT 与签名 TwiML 生成旧响应契约。测试覆盖标准 Basic auth、`sub=Account SID`、跨 uid、provider failure、Edge header/body 保留和删号 fence。真实 Twilio staging credentials/号码验证、历史 Firestore backfill 与 production cutover 仍未完成。
- 新 owner staging 发布（2026-08-31）：远端 App D1 已应用 `0107_wrapped_jobs.sql`、`0108_phone_twilio.sql`；API AI `e0931245-069a-4e4d-a689-9a0abbad39e0`、Jobs `16b22381-cff7-42e4-a8d1-f01a61067fcd`、Edge `356ddb07-fd56-486c-b1f1-5773dddd2757` 已部署。匿名请求对 Wrapped、Phone、Cloudflare chat completion 与 audio merge 均为 `401 unauthorized`；未注入 provider fixture，故不把匿名边界当作正向业务验证。
- 为避免 staging Edge 把 Gemini prompt、用户 BYOK key 或旧 Firebase 身份继续送入 legacy，`GEMINI_PROXY_STAGING_FAIL_CLOSED=true` 时，`/v1/proxy/gemini*` 两个 POST 入口统一返回 `503 gemini_provider_unavailable` 并带 `cache-control: no-store`，不读取请求 body、不调用 legacy。API-AI 已切为 staging owner，承载 bounded Gemini JSON/SSE、Edge DO burst、D1 daily ledger、BYOK enrollment 和 provider alias；这些 adapter 不是 Firebase/旧 Vertex PT/ADC 兼容 owner。真正 production cutover 仍需闭合 Firebase/Better Auth 身份连续性、Redis/Pro quota、BYOK header authority、Vertex ADC/PT 路由、模型/action 白名单、SSE/usage/error wire contract 和 provider cost accounting。非 staging 环境未设置开关时仍保留 legacy forwarding。
- Gemini/chat compatibility fail-closed live probe（2026-09-01）：最新 API-AI/Edge 发布后，Gemini exact JSON/stream 在未配置 provider secret 时均返回 `503 gemini_provider_unavailable`，而 Chat compatibility alias 返回 bounded owner 的稳定 4xx/5xx；请求不会送往 legacy。Gemini 与 Chat compatibility 当前均计入 staging owner，不再计入 legacy-owned；生产 provider、历史 continuity 和完整 wire parity 仍未完成。
- 0126 历史 Phone ledger staging 发布（2026-09-01）：`0126_phone_number_import_ledger.sql` 已应用到远端 App D1；Jobs/Edge/Auth/API Core/API AI/Realtime/Web 全部重新发布，完整 staging smoke 与 Edge/Web health 均为 200，Cloudflare 本地 80 个测试文件/618 个测试通过。Persona/App 与 Phone 回放工具只生成受 fence、checksum、proof 和 encrypted-only 约束的 dry-run 计划，不执行 Firestore/GCS/Twilio 读取或 D1/R2 写入；随后 app-owner exact adapter 已切 staging owner，但 production data/provider parity 仍阻塞。
- `/v2/sync-jobs/run` 已切到 staging Jobs owner：Edge 只转发已验证的 Better Auth 身份，Jobs 只接受有界 `{job_id}`，从 D1 反查当前账号的 lane/lease，并将 queued 或过期 running job 投递到现有 `sync_local_files` Queue；未知/跨账号安全 dropped，terminal acked，活动 lease 返回 409 locked，Queue 不可用返回 503。最新 staging 边界探针已覆盖未认证 `401`、未知 `200 dropped`、queued `200 queued`、终态 `200 acked`，fixture 清理后 Auth/App 均无残留；带真实录音的 Queue drain、旧 Cloud Tasks/OIDC dispatcher 的生产切换和 staging residual 观察仍待完成。Merge 的外部 OAuth/AI fan-out 依赖仍按配置 fail-closed，不能把缺失 provider secret 当作成功。
- `/v1/conversation-finalization-jobs/run` 已切到 staging Jobs owner：Edge 只转发已验证的 Better Auth 身份，Jobs 只接受有界 `{job_id}`，从 D1 反查当前账号的 operation/conversation/revision，并将 queued 或过期 running job 投递到 `conversation_finalize`/`conversation_reprocess` Queue；未知/跨账号安全 dropped，终态 acked，活动 lease 返回 409 locked，Queue 不可用返回 503。最新 staging 边界探针已覆盖未认证 `401`、认证未知 job `200 dropped`、多余 caller 字段 `400 invalid_request`；探针账号清理后 Auth/App 均无残留。该探针验证的是入口和 owner boundary，不等同于真实旧 Cloud Tasks/OIDC dispatcher 已切换或完整 Queue drain。
- `/v1/users/account-deletion-wipes/run` 已切到 staging Jobs owner：Edge 先验证 Better Auth，再由 Jobs 从 D1 intent 反查 UID，只推进当前账号自己的 deletion intent；未知或跨账号 job 只返回 dropped，不执行任何写入。Cloudflare Queue consumer 仍是实际清理 authority，旧 Cloud Tasks/OIDC 生产 dispatcher 尚未切换。
- Desktop Beta 四条 mutation 已成组迁移：reserve/admission、signed GitHub evidence、manifest/pointer CAS 和 breakglass audit 共享 D1 控制面；没有 `BETA_PROMOTION_TOKEN`/`GITHUB_TOKEN` 时 promotion 与 emergency rollout 会 fail-closed，不会产生半写入。发布流水线凭据和历史 Firestore→D1 回放仍是 staging 正向验证前置条件。
- 最新 staging 发布证据（2026-09-01，本轮 0127/0128/0129）：远端 App D1 已应用 `0127_app_owner_data_projection_attestation.sql`、`0128_chat_history_reconciliation.sql`、`0129_phone_number_import_executor.sql`；Jobs `36fdb378-8b14-4021-93e2-a08844160508`、Edge `6e7d090f-5885-460e-9b44-a76ba1723118` 与 Web `7ad38f83-c1c9-4330-8848-4a4fbd58ac31` 已发布，完整 authenticated smoke 与 Edge/Web health=200，Cloudflare 82 个测试文件/631 个测试通过。三组新增能力均保留 generation/deletion fence；app owner data attestation、chat history replay、phone history apply 默认不自动执行，当前尚无 legacy-owned 路由，未声称 Firebase/Firestore 历史、provider/file continuity 或 production wire parity。
- Wrapped history reviewed apply seam（2026-09-01，已发布但默认关闭）：远端 App D1 已应用 `0131_wrapped_history_executor.sql`，review batch/items/apply marker 三张表已存在且无待应用 migration；Jobs 已重部署，完整 Cloudflare suite 为 85 个文件/647 个测试，typecheck 与 manifest validation 均通过。新增批量 generation/deletion-fence 复核、严格 JSON shape/字段白名单和并发 apply 幂等保护，但该 seam 只晋级外部已审阅的 Firestore result plan，仍未执行 Firestore/GCS export、历史账号回放、provider/client parity 或 production cutover。
- 本轮 live boundary 复核：authenticated exact `POST /v1/apps/migrate-owner` 返回 `503 persona_apps_unavailable`/`no-store`，namespaced `POST /v2/cf/apps/migrate-owner/data-projection` 在缺少 admin key 时返回 `503 app_owner_data_attestation_unavailable`；远端 attestation、chat-history ledger、phone review/apply 表均为 0 行，未发生未经审阅的数据写入。
- 浏览器回归（2026-09-01，本轮发布后）：已登录 staging `/conversations` 页面正常渲染 Conversations 空态，未出现 API error/404，浏览器 error/warn 日志为空；与 authenticated smoke 的 `conversations=200` 相符。
