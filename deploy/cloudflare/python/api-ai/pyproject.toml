[project]
name = "omi-cf-api-ai"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi==0.121.0",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
  "pytest",
]
