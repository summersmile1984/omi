version: 1
routes:
  - method: GET
    path: /v1/fair-use/case/{case_ref}/status
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/fair-use/status
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/admin/fair-use/flagged
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/admin/fair-use/user/{uid}
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/admin/fair-use/user/{uid}/resolve-event/{event_id}
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/admin/fair-use/user/{uid}/reset
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/admin/fair-use/user/{uid}/set-stage
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/admin/fair-use/case/{case_ref}
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/me/usage
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/me/subscription
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/payments/available-plans
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /health
    owner: edge
    runtime: edge-worker
    target_runtime: typescript-worker
    auth_authority: public
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /ready
    owner: edge
    runtime: edge-worker
    target_runtime: typescript-worker
    auth_authority: public
    dependencies: [auth, api-core, api-ai, realtime, jobs, rate-limit-do]
    protocol: http
    rollout: staging-only
    rollback: restore-worker-deployment-snapshot
  - method: ANY
    path: /api/better-auth/*
    owner: auth
    runtime: auth-worker
    target_runtime: typescript-worker
    auth_authority: better-auth
    dependencies: [d1-auth]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/firmware/stable
    owner: api-core
    runtime: python-worker
    target_runtime: external-api
    auth_authority: public
    dependencies: [external-github-api]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/firmware/latest
    owner: api-core
    runtime: python-worker
    target_runtime: external-api
    auth_authority: public
    dependencies: [external-github-api]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/firmware/version
    owner: api-core
    runtime: python-worker
    target_runtime: external-api
    auth_authority: public
    dependencies: [external-github-api]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/announcements/changelogs
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/announcements/features
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/announcements/general
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/announcements/pending
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/announcements/:announcementId/dismiss
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/announcements/all
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/announcements/:announcementId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/announcements
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PUT
    path: /v1/announcements/:announcementId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/announcements/:announcementId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: admin-secret
    dependencies: [d1-app, worker-secrets]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/app-categories
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: []
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/app/proactive-notification-scopes
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: []
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/app-capabilities
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: []
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/app/payment-plans
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: []
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/approved-apps
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/apps/popular
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/apps/{appId}
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/apps/review
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/apps/{appId}/review
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/apps/{appId}/review/reply
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/apps/{appId}/reviews
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/apps
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/apps/capability/{capability_id}/grouped
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/apps/search
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/apps/enabled
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/apps/enable
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/apps/disable
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/embeddings
    owner: api-ai
    runtime: python-worker
    target_runtime: external-api
    auth_authority: better-auth
    dependencies: [external-embedding-api]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/embeddings-workers-ai
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [workers-ai-binding]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/config/api-keys
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [worker-secrets]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/tts/synthesize
    owner: api-ai
    runtime: python-worker
    target_runtime: external-api
    auth_authority: better-auth
    dependencies: [external-tts-api, rate-limit-do]
    rate_limit_policy: tts:synthesize
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/tts/synthesize-workers-ai
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [workers-ai-binding, rate-limit-do]
    rate_limit_policy: tts:synthesize
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/auto/model-pick
    owner: api-ai
    runtime: python-worker
    target_runtime: external-api
    auth_authority: better-auth
    dependencies: [d1-app, external-model-ranking-api]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v1/ai/*
    owner: api-ai
    runtime: python-worker
    target_runtime: external-api
    auth_authority: better-auth
    dependencies: [external-ai-api]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v2/messages
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, workers-ai-binding, rate-limit-do]
    rate_limit_policy: chat:send_message
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v2/messages/share
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v2/messages/shared/:token
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/stt/transcribe
    owner: api-ai
    runtime: python-worker
    target_runtime: external-api
    auth_authority: better-auth
    dependencies: [d1-app, external-asr-api, rate-limit-do]
    rate_limit_policy: stt:transcribe
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v2/voice-message/transcribe
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, workers-ai-binding, rate-limit-do]
    rate_limit_policy: stt:transcribe
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v2/realtime/session
    owner: api-ai
    runtime: python-worker
    target_runtime: external-api
    auth_authority: better-auth
    dependencies:
      [d1-app, external-openai-realtime-api, external-gemini-live-api]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v2/realtime/usage
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/realtime/web-ticket
    owner: edge
    runtime: edge-worker
    target_runtime: typescript-worker
    auth_authority: better-auth
    dependencies: [d1-auth, worker-secrets]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/stt/transcribe-async
    owner: jobs
    runtime: edge-worker
    target_runtime: typescript-worker
    auth_authority: better-auth
    dependencies: [d1-app, r2, queue-jobs, workers-ai-binding, rate-limit-do]
    rate_limit_policy: stt:transcribe
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/stt/transcribe-async/:jobId
    owner: jobs
    runtime: edge-worker
    target_runtime: typescript-worker
    auth_authority: better-auth
    dependencies: [d1-app, r2, queue-jobs, workers-ai-binding]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/stt/transcribe-workers-ai
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, workers-ai-binding, rate-limit-do]
    rate_limit_policy: stt:transcribe
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/translate
    owner: api-ai
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [workers-ai-binding]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/transcription-preferences
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/transcription-preferences
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/available-languages
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: []
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/language
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/language
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/onboarding
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/onboarding
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/store-recording-permission
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/store-recording-permission
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/private-cloud-sync
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/private-cloud-sync
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/training-data-opt-in
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/training-data-opt-in
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/fcm-token
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v1/users/developer/webhook/*
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/developer/webhooks/status
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/notification-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/notification-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/daily-summary-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/daily-summary-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/daily-summaries
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/daily-summaries/:summaryId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/daily-summaries/:summaryId/visibility
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/users/daily-summaries/:summaryId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/daily-summary-settings/test
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/daily-summaries/:summaryId/regenerate
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/mentor-notification-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/mentor-notification-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/geolocation
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/action-items
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/action-items
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/action-items/ids
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/action-items/batch
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/action-items/batch
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/action-items/batch-delete
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/action-items/pending-sync
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/action-items/sync-batch
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/action-items/share
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/action-items/shared/:token
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: public
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/action-items/accept
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/daily-score
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/scores
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/focus-sessions
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/focus-sessions
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/focus-sessions/:sessionId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/focus-stats
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/screen-activity/sync
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/screen-activity
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/screen-activity/summary
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/calendar/onboarding/status
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/calendar/onboarding/skip
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/calendar/onboarding/reset
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/calendar/meetings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/calendar/meetings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/calendar/meetings/:meetingId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/action-items/:actionItemId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/action-items/:actionItemId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/action-items/:actionItemId/completed
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/action-items/:actionItemId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/users/people
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/people
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/people/:personId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/people/:personId/name
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/users/people/:personId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/goals
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals/all
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals/canonical/list
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/goals/canonical
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals/:goalId/detail
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/work-intents
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/workstreams/:workstreamId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/workstreams/:workstreamId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/workstreams/:workstreamId/events
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/workstreams/:workstreamId/events
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/workstreams/:workstreamId/artifacts
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/workstreams/:workstreamId/artifacts
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/workstreams/:workstreamId/checkpoints
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PUT
    path: /v1/workstreams/:workstreamId/checkpoints/:runtimeId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/workstreams/:workstreamId/artifacts/:artifactId/status
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals/:goalId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/goals/:goalId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/goals/:goalId/progress
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals/:goalId/history
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/goals/:goalId/progress-events
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/goals/:goalId/progress-events
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/goals/:goalId/focus
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/goals/:goalId/focus
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/goals/:goalId/lifecycle
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/goals/:goalId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/folders
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/folders
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/folders/:folderId/conversations
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/folders/:folderId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/folders/:folderId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/folders/:folderId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/folders/reorder
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/folder
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/assistant-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/assistant-settings
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/ai-profile
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/users/ai-profile
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/profile
    owner: auth
    runtime: auth-worker
    target_runtime: typescript-worker
    auth_authority: better-auth
    dependencies: [d1-auth]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/users/location-context-consent
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PUT
    path: /v1/users/location-context-consent
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v2/voice-message/transcribe-stream
    owner: realtime
    runtime: realtime-do
    target_runtime: realtime-do
    auth_authority: better-auth
    dependencies: [durable-object, d1-app, external-asr-api]
    protocol: websocket
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v4/listen
    owner: realtime
    runtime: realtime-do
    target_runtime: realtime-do
    auth_authority: better-auth
    dependencies: [durable-object, d1-app, external-asr-api]
    protocol: websocket
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v4/web/listen
    owner: realtime
    runtime: realtime-do
    target_runtime: realtime-do
    auth_authority: better-auth
    dependencies: [durable-object, d1-app, external-asr-api]
    protocol: websocket
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v1/omni/relay
    owner: realtime
    runtime: realtime-do
    target_runtime: realtime-do
    auth_authority: better-auth
    dependencies: [durable-object, d1-app, external-asr-api]
    protocol: websocket
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v1/cf/assets/*
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, r2]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/conversations/search
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: conversations:search
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v3/memories
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v3/memories
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:create
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v3/memories
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:delete_all
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v3/memories/batch
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:delete_batch
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v3/memories/:memoryId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:delete
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v3/memories/:memoryId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:modify
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v3/memories/:memoryId/visibility
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:modify
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v3/memories/:memoryId/review
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, rate-limit-do]
    rate_limit_policy: memories:modify
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/account/cutover/control
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/count
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/conversations/:conversationId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId/photos
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId/transcripts
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId/analytics
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/action-items
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/action-items/:actionItemIdx
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/conversations/:conversationId/action-items
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId/recording
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app, r2]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/events
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/summary
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: DELETE
    path: /v1/conversations/:conversationId/calendar-event
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/segments/text
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/title
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: PATCH
    path: /v1/conversations/:conversationId/starred
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId/action-items
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/conversations/:conversationId/action-items/count
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/cf/conversations
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/cf/conversations
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: optional
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/cf/conversations/count
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/cf/conversations/:conversationId
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: POST
    path: /v1/folders/:folderId/conversations/bulk-move
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    idempotency: required
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: ANY
    path: /v1/cf/probe
    owner: api-core
    runtime: python-worker
    target_runtime: python-worker
    auth_authority: better-auth
    dependencies: [d1-app]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
  - method: GET
    path: /v1/cf/jobs/:jobId
    owner: jobs
    runtime: jobs-queue
    target_runtime: typescript-worker
    auth_authority: better-auth
    dependencies: [d1-app, queue]
    protocol: http
    rollout: staging-only
    rollback: edge-route-to-legacy
