{
  "area": "flutter",
  "comment": "Fork-owned extension to .github/scripts/dead_code/flutter.allowlist.json: the seven staged identity files in app/lib/fork/identity/ are wired into the Android/iOS debug artifact by app/fork/prepare.py rather than imported by any source-tree file. They are intentional fork code staged until either (a) upstream accepts the identity-provider seam PR and removes these entries, or (b) the files are rewired to be live in source. Discovered by scripts/fork/check_dead_code_fork.py which loads both files.",
  "entries": [
    { "path": "app/lib/fork/identity/client.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" },
    { "path": "app/lib/fork/identity/credential.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" },
    { "path": "app/lib/fork/identity/deployment.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" },
    { "path": "app/lib/fork/identity/owner.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" },
    { "path": "app/lib/fork/identity/runtime.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" },
    { "path": "app/lib/fork/identity/secure_store.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" },
    { "path": "app/lib/fork/identity/sign_out.dart", "reason": "fork-staged Better Auth: wired by app/fork/prepare.py into the staged artifact; see T1 hook budget" }
  ]
}
